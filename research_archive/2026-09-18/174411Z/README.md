# Twisted-chiral campaign: first preservation checkpoint

Snapshot at 17:44:11 UTC; the bounded campaign continues until 20:09:11 UTC.
All 2,115 research payloads passed SHA-256 verification. Drafts, unsuccessful
experiments, intermediate outputs, and inverse-reconstruction witnesses are
preserved without being promoted to proofs. Canonical current work remains in
`artifacts/twisted_chiral_*` and `computations/twisted_chiral_*`.

No credential candidate or unpreserved oversized research output was found.
Environments, caches, dependencies, and reviewed reproducible binaries are
excluded. The build-provenance review is
`research_archive/twisted_chiral_build_audit_2026_09_18.md`.
The manifest's unresolved dynamic-reference flags are inventory warnings,
not claims of theorem dependency closure; the campaign's exact computations
use tracked matrix inputs and documented source-built executables. Scratch
inverse-search outputs are included in this snapshot.

The pre-existing large rank-two JSON remains untracked because a lossless
tracked archive already preserves it. The pre-existing exact-range ELF is a
reviewed reproducible build product. Concurrent searches continue to generate
new research, which will be preserved at the next checkpoint.
