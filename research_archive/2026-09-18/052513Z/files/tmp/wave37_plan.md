# Wave 37 ranked ideas

Evidence basis: README, Wave 36 Updated frontier (§10.89.5), and STEERING.md
were reread after commit b068dcf.  No decisive route change; steering refresh
remains due at Wave 39.

1. **Paired block-replacement -> completion width.**  Replace a principal
   block by both signs/orientations of an exact child minimizer and use exact
   global signing minimality to force one *favorable projective* fiber with
   small width deficit `b_S`; explicitly defeat the A6/A9 witness-migration
   wall and test the needed `E b=O(n(d+1))` scale.
2. **Discrete-minimality compression after the rare-tail converse.**  Combine
   the full simultaneous edge-flip/deep-hole cover (10.733), not merely its
   one-edge marginals, with (10.1022).  Seek a genuinely signing-specific
   compression/weighted-congestion theorem for row-good favorable cut
   incidences, or an exact obstruction showing which new statistic is absent.
3. **Matched no-transient-mode-transfer identity.**  Differentiate the
   selector posterior/exclusion law along the actual matched parent mixture;
   express adverse context migration in selector-information or conditional
   partition terms that the abstract redundant-bit wall does not satisfy.
4. **Joint width/row completion.**  For a favorable local label, derive the
   exact relation among completion width, `y^T(A^2)[S]y`, and the minimum row
   square over outside completions.  Determine whether small `b_S` can also
   supply the row-qualified median instead of treating row as independent.
5. **Actual-minimizer favorable row transfer.**  Strengthen (10.1033) using
   multi-edge signing minimality to control the cubic selector tensor.  Avoid
   cap/local-stability arguments already falsified by the hub joins.
6. **Low-row incidence metric entropy.**  Do not count all low-row cuts
   (Markov makes them abundant).  Bound the number/entropy of *distinct
   favorable selector incidence patterns* realized by row-good cuts, then
   combine the bound with (10.1022) under a hypothetical normalized gap.
7. **Cost-weighted anti-evanescence.**  Replace global
   `osc(log f)` in (10.1040) by an edge-cost-weighted local oscillation or
   conditional log-partition modulus; prove it for quadratic matched fibers.
8. **Rare-center mismatch compression from discrete block flips.**  Apply
   exact minimality to the mismatch-pattern partition in (10.996) and seek a
   weighted bound on `sum_B exp(gamma Delta_B R)` that uses changed witnesses,
   not the old-ground normal cone.
9. **Probabilistic row-good completion of coherent labels.**  Optimize the
   free coordinates of a plurality/median by vector balancing; target an
   `O(n^(9/4-c))` completion while preserving selector distances.  First
   identify the external Gram obstruction exactly.
10. **Independent global construction/interpolation route.**  Search primary
    literature and test whether covering-code products, interpolation, or
    approximate subadditivity can establish convergence directly at the
    `n^(3/2)` scale despite the exact block-gluing obstruction (§1.6).

Selected parallel attacks: 1, 2, 3.  The main agent pursues 4 and 10, and
audits all outputs.  Ideas 5--9 are successors if one selected mechanism
closes or yields a sharper reduced lemma.
