# Boolean BH boundedness campaign: preservation and publication audit

Campaign: 2026-09-18, 04:27:50--07:27:50 UTC. The original signing problem
remains paused. Preservation and publication do not assert that BH boundedness
or divergence has been resolved; see the canonical final synthesis.

## Snapshots

| Snapshot | Preserved research payloads | Verification |
| --- | ---: | --- |
| 052513Z | 1,979 | Payload hashes checked; first substantive checkpoint |
| 062116Z | 1,984 | Payload hashes checked; continuing research |
| 070738Z | 1,983 | Every payload SHA-256 checked; 169,063,859 bytes |

The final snapshot includes the thirteen-program closing replay report,
all 26 source hashes, the director's independent closing proof audit,
primary sources, exploratory searches, failed compiler mechanisms,
counterexamples and raw numerical outputs. No credential candidate or
unresolved oversized research output was flagged. No original was removed.
Canonical closing edits after the snapshot are preserved directly in Git.

## Proof and dependency checks

The 26 BH programs depend only on explicit seeds/constants, the standard
library, numpy/scipy/sympy/mpmath, or named tracked sibling modules. They
do not require ignored mathematical input files. Runtime versions are in
the snapshot's runtime.json, which is observed metadata, not a tested lockfile.
The replay runner sets temporary directories inside the workspace.

All thirteen selected closing replays returned zero. Exact Eisenstein and
Gaussian-integer arithmetic, rational margins and finite-field minors retain
their exact labels; floating-point, high-precision and eigenvalue diagnostics
do not become proofs by passing. The address-tangent program was additionally
replayed after its exact parity-restriction test was added and also passed.

Three independent synthesis audits and the director's reconstruction checked
the changing BH exponent, field distinction, all-order versus subsequence
limit, fixed-seed versus moving-seed scope, and ambient dimension in the
coefficient-error comparison. The ABKRT commutator ingredient is credited to
its primary source; no external priority claim is made for our adaptations.

The conservative repository-wide ignored-reference scan retains the same
14 historical/dynamic flags. They are not new dependencies of these proofs.
A literal reference scan does not certify arbitrary dynamic dependency closure.

## Intentionally retained untracked or ignored material

- `computations/decisive_independent_exact_range_scan_2026_09_07`:
  previously reviewed reproducible executable, excluded as a build product.
- `computations/results/flatify_director_rank_two_witness_profiles_2026_09_07_m32.json`:
  the 176,489,665-byte working original, already byte-verified against a tracked
  lossless compressed archive and checksum manifest.
- Scratch research originals remain in place and are preserved by the dated
  snapshot. Environments, caches, reviewed dependencies and reproducible build
  products are excluded for the existing documented reasons.

No research-bearing file is omitted for being unfinished, negative or unpolished.
The post-publication current-work audit must confirm that remaining ignored
research matches its saved payload; final canonical status edits are committed
directly rather than duplicating the snapshot.
