# Preservation checkpoint — 2026-09-06 19:29:59 UTC

1,901 research payloads, 154,677,428 bytes. **Preservation does not verify
claims.** Canonical proofs and certificates remain where they were; this
snapshot retains working drafts, failed comparisons, exploratory programs,
parameters, outputs, and agent reports without polishing.

This checkpoint includes the new discrete-threshold entropy theorem and
finite-quantizer extension, their independent audits, all-fixed-retention
first-exit proof, and the Paley isotropic-ground investigation. The director's
unproved replica comparison and bounded Sidon-literature reconnaissance are
explicitly retained as notes, not upgraded to proved claims.

## Exclusions and recovered preservation gap

The [full build audit](../../build_product_audit_2026_09_06.md) accounts for
27 research ELF files and 50 bytecode files. One binary, `tmp/search_r33`,
has no recovered source and is preserved in this snapshot: 21,808 bytes,
SHA-256 `b02af09feaac26707ff606feaec819d71434d119fb8ee3af6ced4bf884915743`.
The other 26 executables have recorded source mappings/build recipes;
the archiver excludes them only while both binary and source hashes match
the reviewed record. New or changed binaries require fresh review.

Environment/cache files (17,596 entries), 76 reviewed build/bytecode products,
and eight environment symlink references remain excluded. No credential
candidate or oversized research output was detected. No material was deleted.
Historical build recipes are not claims of identical historical compiler bytes.

## Dependency audit

All five new canonical checkers were replayed by
`computations/transfer_director_checkpoint4_replays_2026_09_06.py`.
Inputs are explicit constants or generated matrices, not ignored data.
The cap-integration checker imports the tracked elementary interval module
and the new canonical rational-threshold checker. Python/NumPy/SciPy
environment versions are recorded, not presented as a tested lockfile.

The conservative reference scan has 43 literal temporary-path occurrences:
24 preserved research references, 12 reviewed binary references, two directory
references, and the same five missing/dynamic examples previously reviewed.
The [initial dependency audit](../173630Z/README.md#dependency-audit) still
documents the two historically missing chiral source modules and explicit
external Hadamard input. Those limitations remain visible; they are not
dependencies of the current lower or upper bound proofs.

No concurrent-copy warning was detected. Live agents continue working;
post-snapshot material will be preserved at the next substantive checkpoint.
Untracked working originals are deliberately retained, not a failed archive.
