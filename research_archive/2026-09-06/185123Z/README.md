# Preservation checkpoint: 2026-09-06 18:51:23 UTC

This snapshot preserves 1,915 research-bearing files, totaling 154,762,575
bytes, with SHA-256 and size verification. It includes exploratory scripts,
unfinished calculations, numerical probes, failed hypotheses, subagent
reports, parameters and outputs. Snapshot inclusion is NOT proof verification.
Canonical proof status is in `artifacts/transfer_campaign_2026_09_06.md`.

## Scope and exclusions

The manifest records each original path, hash, byte count and working-tree
provenance. Canonical files have not been moved. Working originals remain
available to active researchers. The snapshot excludes 17,595 environment or
cache files, 77 reproducible build products, and eight environment/reference
symlinks. No credential candidates or research files above the 25 MiB review
threshold were found. No research output required an external-storage exception.

## Dependency review

The fourteen new canonical regression programs were replayed successfully.
Their inputs are generated from explicit in-source parameters; their local
interval helper, `continued_feedback_ternary_latent_interval_certificate_2026_09_06.py`,
is already tracked. Standard-library, NumPy and SciPy imports are recorded
with observed installed versions in `runtime.json`. No new ignored research
input is required by those checks. Primary mathematical imports are cited in
the proofs and are not undeclared local cache dependencies.

The conservative literal temporary-path scan still has 42 references: 23
resolve to preserved research, 12 to reproducible binaries, two to directories,
and five are the same manually reviewed missing/dynamic examples documented
at the first snapshot. See
`../173630Z/README.md#dependency-audit` for that review and the two unavailable old
chiral source modules. Their missing source was not fabricated or silently
treated as a replayable proof. The current upper/lower certificate chains
do not depend on those old modules.

## Remaining working files

Archived originals remain untracked or ignored intentionally; deleting them
would disrupt research without improving preservation. Newly verified proofs
and checkers are committed canonically at this checkpoint. Exploratory and
unfinished work remains in its working location and in this dated archive.
Anything produced after this snapshot is active work for the next substantive
preservation checkpoint. Environments, caches and rebuilt binaries remain
uncommitted by policy; no original research file was deleted.
