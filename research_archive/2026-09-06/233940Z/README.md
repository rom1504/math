# First decisive-limit campaign preservation checkpoint

Snapshot time: 2026-09-06 23:39:40 UTC. Preserves 1,932 research files,
155,077,507 bytes, without deleting working originals. Drafts and failed
experiments are retained without requiring polish or positive results.
Canonical proofs and exact certificates remain in artifacts/ and computations/.

Mathematical status is not implied by preservation. The new precision Schur
theorem and all-order upper reconstruction are separately audited; numerical
diagnostics and exploratory claims retain their own labels.

Excluded: 17,599 environment/cache files; 76 reviewed build products;
25 pinned external dependency files; eight environment symlink references.
No credential candidate or research output over the review size threshold
was detected. The newly compiled small-order enumerator is preserved until
its build provenance is separately classified; no executable is silently lost.

The conservative ignored-reference scan retains 24 research references,
14 reviewed build references, five missing/dynamic references and two import
directories. The known historical missing modules remain documented in the
earlier publication audit; no new precision or constant replay depends on them.
The new exact interval replay uses its tracked certificate program and the
venv standard/package dependencies, not an ignored result file.

See manifest.json for original paths and SHA-256 hashes, runtime.json for
observed package versions, and ignored_references.json for literal references.
This checkpoint is a preservation event, not the end of the research run.
