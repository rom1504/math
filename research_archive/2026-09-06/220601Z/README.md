# Preservation checkpoint — 2026-09-06 22:06:01 UTC

Preservation only: **1,915 research files, 154,814,524 bytes**. All research-
bearing untracked/ignored files and modified tracked research were captured
without requiring polished proofs. Canonical files remain in place; working
originals were not deleted. Source paths, SHA-256, sizes and status are in
`manifest.json`; the six earlier snapshots retain their own historical states.

This snapshot includes the final cap-only energy-transport proof, finite
rich-feedback lemmas, explicit moment/alias/degree counterexamples, exploratory
scripts and old failed searches. The complete rich-feedback assembly is still
OPEN. Inclusion in this archive is not verification or promotion of a draft.

## Exclusions and known gaps

- 17,597 environment/cache files, 577,880,413 bytes.
- 76 reviewed compiled/bytecode products, 1,441,864 bytes. The source-less
  `tmp/search_r33` executable remains preserved as research.
- 25 byte-identical downloaded dependencies, 6,996,782 bytes; pinned upstream
  commits, durable URLs and hashes are in the archive-wide dependency manifest.
- Eight symlink references, with targets recorded and not followed.
- No detected credential candidates or oversized research requiring a separate
  durable storage decision. The pattern scan is not a universal secret detector.

The 43 literal ignored-path references resolve to 24 preserved research files,
12 reviewed build products, two directory/search paths, and five historical
missing/dynamic references. Two old unavailable source modules are explicitly
documented in the initial snapshot; they were not reconstructed or silently
treated as reproducible. They do not enter the present rigorous interval.

All six new replay programs are standalone except for documented Python
packages and the replay runner's tracked source programs. Their outputs record
seeds, exact case counts or parameters and source hashes. No ignored research
file is an input to these programs. New mathematical dependencies are tracked
proof artifacts or explicitly linked primary sources. External catalogue
completeness remains an optional, documented non-replayed dependency.

The automatic scan only finds literal ignored paths, not arbitrary dynamic
imports. Manual review of current proof/replay chains supplements it. All
payload hashes are verified before publication; `--audit-current` additionally
checks that remaining uncommitted research has not changed since this snapshot.
Any later work must be committed canonically or captured in a newer snapshot.

The current-work audit detected two late amendments at 22:07 UTC: the new
rich mean-linear Hall audit and an appended local-contraction replay audit.
Both are preserved in their canonical `artifacts/` paths in the publication
commit accompanying this snapshot. The older snapshot payloads are unchanged.
The final current-work audit is run after that commit so it checks remaining
uncommitted research rather than requiring duplicates of committed updates.
