# Preservation checkpoint — 2026-09-06 21:14:00 UTC

Preservation only: **1,903 research files, 154,918,023 bytes**. Research
knowledge in temporary/untracked locations is retained regardless of proof
status. Source paths, hashes, sizes and provenance are in `manifest.json`.
Canonical material stays in its original location; no working original was
removed. The smaller count than the previous snapshot reflects promotion of
files to tracked canonical status, not a deletion of research.

The campaign checkpoint separately records the independently proved same-
order orientation repair and fixed-first-history actual-feedback gain.
Six replay programs distinguish exact integer/rational results from floating
diagnostics. The snapshots also keep failed catalyst searches, the unresolved
exact-minimizer dual attempt and newer agent drafts. Preservation is not a
verification upgrade.

## Exclusions and dependencies

- 17,597 environment/cache files, 577,880,413 bytes.
- 76 reviewed compiled/bytecode products, 1,441,864 bytes. Source mappings
  remain in the archive-wide build audit. The source-less experiment binary
  remains research, not an excluded allegedly reproducible product.
- 25 unchanged external downloaded dependencies, 6,996,782 bytes, with
  pinned commit/URL/SHA manifest. No new third-party dependency was introduced.
- Eight symlink targets recorded without traversal.
- No detected credentials or oversized research awaiting durable storage.

The 43 literal ignored-path references are unchanged: 24 preserved research,
12 reviewed build products, two directory/search paths and five old missing
or dynamic references. Their known historical boundaries remain documented
in the first snapshot. The scan is conservative, not a dynamic code proof.

New current scripts either have no input files, use tracked finite signing
matrices, or read the tracked exact minimizer-isotropy and external-witness
JSON data. Proof imports are existing tracked artifacts. The optional old
external catalogue assessment remains explicitly download-dependent; it is
not required by the signing bounds or new feedback theorem. Current canonical
proof/replay chains have no undocumented ignored research input.

All payload SHA-256 values were checked before publication. Active agents
continue after this checkpoint; any newer draft is captured at the next
snapshot rather than being silently treated as disposable.
