# Preservation checkpoint — 2026-09-06 20:35:22 UTC

Preservation only: **1,916 research-bearing files, 155,235,485 bytes**.
Each payload has its original path, SHA-256, size and source category in
`manifest.json`. Drafts, failed solvers, intermediate calculations and newer
subagent reports are retained without a claim of verification. Original
working files remain in place. Canonical proofs and certificates are not moved.

The accompanying campaign checkpoint distinguishes proved orientation repair,
counterexamples and exact finite certificates from open convergence arguments.
Seven checkpoint programs were independently replayed by the director; their
complete arguments and outputs are in the tracked checkpoint-five replay JSON.
The order-15/16 witness verifier also replayed the preserved numeric matrices
without the external download cache. Neither run certifies an external global
order-15/16 optimum.

## Exclusions and recovery

- 17,596 environment/cache files (577,866,828 bytes).
- 76 reviewed build products (1,441,864 bytes): source/build mappings are in
  the archive-wide build audit. The source-less `tmp/search_r33` stays preserved.
- 25 byte-identical external downloaded dependencies (6,996,782 bytes), with
  pinned upstream commit, durable URL and SHA-256 in
  [the archive-wide manifest](../../reviewed_external_dependencies.json). Their upstream
  license is unspecified. Local copies remain; our verifiers, numerical
  witnesses, audit notes and informative outputs are tracked separately.
- Eight symlink targets are recorded, not recursively traversed.
- No credential candidates or oversized research files were detected. This is
  not a claim that automated pattern scanning proves absence of secrets.

Exclusions use exact hashes, not a blanket external-research directory rule.
Changed or newly authored research will be included at the next checkpoint.

## Dependency audit

The literal ignored-reference scan still has 43 hits: 24 preserved research
files, 12 reviewed build products, two search/output directories and five old
missing or dynamic references. The initial checkpoint documents the two old
unavailable chiral-search source modules; they have not been invented or
silently marked recovered. This scan does not establish dynamic dependency
closure.

New canonical finite scripts read the tracked minimizer-isotropy JSON and/or
the tracked external-witness numeric JSON, and import explicitly tracked
rational-certificate helpers. The optional external catalogue assessment
requires the separately documented pinned download; it is not a dependency of
the independent witness proof or of either asymptotic bound. The current
lower and strict all-order upper proof chains have no undocumented ignored
research input. Historical experiment replay may require restoring a payload
to its original path using `preserve.py --restore-path`.

All payload hashes were checked before publication. Remaining untracked
working originals are intentionally retained and duplicated here, not lost;
active agents can create newer drafts, to be captured at the next checkpoint.
