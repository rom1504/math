# Independent audit and a quantitative energy-capture obstruction

2026-09-07. Source:
`flatify_independent_2026_09_07_partition_localized_bridge.md`. PASS for
the actual finite operation and its all-order scope. Its illustrative
strong energy-capture hypothesis can additionally be ruled out, as below.

## Finite construction and padding

For C=H_L tensor H_k, applying C to a padded chunk-constant vector gives
support on only L coordinates, because the inner normalized Hadamard maps
one to k e1. Zero-extended physical spins and their chunk projections
remain in the cube. Thus each of the three terms containing a chunk-
constant factor has magnitude at most sqrt(LNn)<=N sqrt(L).

For an original chunk of size s<=k, its original projection energy minus
its padded projection energy is at most s(1-s/k)<=k-s. Summing bounds the
loss by the actual padding count D=N-n. This proves the source's finite
bound with its factor three and original (not padded) projection energies.

If R_n*k_n=o(n) and k_n tends to infinity, the chunk counts equal n/k_n
plus O(R_n), and a relatively dense outer Hadamard order gives N/n->1.
Both padding and 3N sqrt(L)/n^(3/2) vanish uniformly, including at zero
residual. Sign-pattern atoms for r centers give at most 2^r classes,
so r<=(1-epsilon)log2(n) and k of order n^(epsilon/2) satisfy all hypotheses.
There is no fictitious simultaneous orthogonal change of the Boolean cube.

## The illustrative vanishing-defect capture is impossible

Let ell denote the banked universal lower coefficient for original full
signs, ell=.4333221116640807, as reconstructed in
`decisive_audit_fresh_full_lower_chain_2026_09_07.md`.
For ANY hollow full-sign child A_n, ANY affordable partition sequence of
the source, and any coefficient c_n<=1/2, define its uniform capture defect

    Delta_n=max_x [|H_A(x)|/n^(3/2)-c_n*A_x/n].

The pointwise capture inequality with additive Delta_n holds by definition.
Use TWO copies of this actual child and the partition-localized bridge.
For u=A_x/n,v=A_y/n, their actual parent cap is at most

    [c_n*(u+v)+sqrt((1-u)(1-v))+2Delta_n+o(1)]n^(3/2)
      <=[1+2Delta_n+o(1)]n^(3/2).

The parent is a literal full sign matrix of order 2n. Its cap is therefore
at least M_(2n)>=(ell-o(1))*(2n)^(3/2). Rearranging proves

    liminf Delta_n >= sqrt(2)*ell-1/2
                       = .11281000719149... .

This argument does not assume the children are minimizers or that their
normalized cap converges. It uses the actual bridge operation and the
existing lower theorem. In particular the source's proposed uniform
vanishing-error hypothesis with coefficient at most one half is not merely
unproved: it is impossible, even for growing affordable partitions.

The conclusion is restricted to this strong linear projection-energy
capture form. It does not rule out a richer signed energy/projection
profile, a joint bridge-child argument, or a global operation changing
the old internal edges. The finite and all-order bridge constructions
themselves remain positive, valid operations.
