# Wave 24 ranked ideas

Read against the Wave 23 frontier and `STEERING.md` cutoff §10.76.

1. Joint row-regular coverage minimax: put the row-square cap inside the
   adversarial selector/ground game and derive its exact dual from switching
   minimality. Seek a single inequality proving both mass of regular grounds
   and capped threshold coverage, avoiding the `A_9` separation wall.
2. Full tilted-tail interpolation: use (10.751)--(10.752), conditional outside
   KL (10.746), and the parent marginal to control the entire exponential tilt,
   not variance at `t=0`. Seek a truncation/entropy inequality that proves
   (10.727) or a rigorously weaker power-saving form.
3. Complete-signing Gibbs congestion: combine several coordinate competitor
   inequalities before marginalization and retain shared edge variables. Seek
   an upper bound on a competitor partition sum, or a complete-signing
   obstruction showing that the abstract parity star wall is unrealizable.
4. Delsarte/Krawtchouk dual for regular deepest coset leaders: formulate the
   row-regular ground codebook as a punctured augmented-cut-code covering
   problem and exploit complete-graph association-scheme constraints absent
   from arbitrary set cover.
5. Entropy-regularized ground selection: choose a parent-ground law minimizing
   row-square exposure plus selector loss and prove a PAC-Bayes/minimax bound
   from the exact identities `sum r_i=q_n` and `sum r_i^2`.
6. Multi-scale row stratification: replace one hard `R_infinity` cap by dyadic
   row classes and a class-dependent mgf parameter, hoping entropy pays for
   rare heavy classes without the critical-scale trimming loss.
7. Ground-vector convex geometry: use the exact-minimizer operator bound and
   covariance of ground cuts to force a large regular face with selector
   overlap; search for a separation theorem whose dual would contradict
   switching minimality.
8. Selector-dependent temperature: optimize beta or an outside-field tilt as
   a measurable function of `S`, and account for its description cost in
   (10.727), to prevent rare outside weights from cancelling child penalties.
9. External-surplus upper tail: derive a minimizer-specific exponential tail
   for the max-plus replacement surplus and feed it into the causal Hall-flow
   formulation, retaining an independent non-pressure route.
10. Focused finite falsification: enumerate exact minimizers at the next
    feasible orders and solve the joint regular capped game, looking only for
    a structural dual certificate or asymptotic obstruction rather than more
    unexplained decimal data.

Selected: 1, 2, and 3. They test the leading implementation, its general
fallback, and the strongest independent local alternative, respectively.
