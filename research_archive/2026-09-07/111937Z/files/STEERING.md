# Strategic steering

Evidence cutoff: fourth material checkpoint, 2026-09-07 approximately 09:40 UTC.
Status: **new six-hour constructive campaign ACTIVE**.
Started 2026-09-07 05:27:43 UTC from fe8fb43; target finish 11:28 UTC.
The preceding campaign completed at 04:45 UTC. Convergence remains OPEN.

## User objectives and authorization

Determine whether M_n/n^(3/2) converges, without presuming its value.
The user authorized six additional active hours, independent invention,
parallel mathematical work, reconstruction of dependencies, and preservation
and normal Git publication at substantive checkpoints. Checkpoints are not
stopping events. Research-bearing drafts, failures and outputs must be archived.
Never use /tmp; use /home/math/quadra/tmp. Writes, execution and push work.
These are user directives; the mathematical judgments below are agent-owned.

New authorization, 2026-09-07: six additional active hours, initially attacking
actual favorable flatification with global internal-edge changes. Use three
complementary researchers and direct mathematical work by the main agent;
independently reconstruct any claimed solution. Continue through checkpoints.
Write access was verified at the start and by this recorded edit.

## Verified frontier

```math
0.4333221116640807
\le\liminf_n M_n/n^{3/2}
\le\limsup_n M_n/n^{3/2}<0.493608094.
```

The new exact upper expression is approximately .493608093588748653.
Convergence to 1/2 is excluded, but the ensemble is not asserted optimal.

[Closing synthesis](artifacts/decisive_campaign_final_synthesis_2026_09_07.md)
is the entry point; ledger 10.151 and Git history retain the detailed record.

The upper proof now needs only: precision Schur BE<=E; direct stopping at E;
the ternary bound E_(97/20)(nu_(24/25))<=-5151/6250; full-spin weave counting;
uniform terminal orbital estimates; H2/H12 all-order realization.
Independent covers (43021 and 21538 rectangles) and a rational final
conversion were replayed. Gaussian-phase equality is not claimed here.
Fix margin, then finite depth, then all orders,
then remove margin. Equality H=E is not a necessary upper-bound premise.
[Standalone proof](artifacts/decisive_audit_standalone_direct_E_upper_2026_09_07.md).
[New parameter proof](artifacts/flatify_independent_2026_09_07_ternary_upper_proof.md)
and [independent reconstruction](artifacts/flatify_adversary_2026_09_07_ternary_upper_reconstruction.md).

The lower chain and its policy certificate were freshly reconstructed and
replayed. Fixed finite policy and approximation precede order; the spectral
deletion cutoff is removed last. The marked argument also proves the same
lower constant for minimum half-range W_n and twice it, .8666442233281615,
for the square full-sign Gale--Berlekamp norm. Bipartite support is explicit.

## Agent-authored assessment

Checkpoint 4: an actual center-conditioned rank-two sign bridge controls
the declared noisy-response sector for arbitrary center spectra. Exact
Schur cancellation, not separately paid channels, yields the improvement.
A directed continuum certificate and padding give all-order sector control
at c>=.47. Exceptional pairs and arbitrary child energy windows remain
unpaid. Actual noisy optimal-child shells rigorously defeat the unconditioned
fixed-pair shell-union certificate. Neither statement proves favorable
flatification. See `artifacts/flatify_director_checkpoint_four_2026_09_07.md`.

Checkpoint 3: rank-two Hadamard tiles are a new actual full-sign operation,
escaping the universal matching floor of reciprocal rank-one weaves. A
joint four-coordinate kernel yields a rigorously audited finite typed-profile
certificate with o(N) type overhead and O(N^(5/4)) fibre completion.
Its growing-order certificate is OPEN; finite values around .48046 are not
asymptotic upper bounds. Random column phases erase the input seed, so this
is not near-optimal seed transfer. The regular-support extension of the
existing upper construction independently proves a square full-sign
bilinear upper constant below .987216188 at all orders. Neither theorem
has paid for actual-child shell alignment or proved convergence.
See `artifacts/flatify_director_checkpoint_three_2026_09_07.md`.

The campaign removed a real analytic alignment obligation and substantially
improved the original all-order upper bound. It did not complete any original
liminf-to-all-order comparison. Further certificate optimization is not the
same as identifying the optimal asymptotic value.

The strongest new actual operation is target contraction followed by rounding:
for a full signing A and symmetric Delta of nuclear norm L, an actual full
signing has cap at most Q(A-offdiag Delta)+O(n sqrt L+n). It has no input
cap/incoherence premise. L=o(n) gives subleading loss. This is a same-order
operation, not flatification: the two-child target is Frobenius distance
Omega(n) from every full signing, so this small-budget theorem cannot directly
bridge it. Optimizer-derived covariance balance is valid but not decisive:
explicit suboptimal Hadamards satisfy much stronger balance, and any fixed
normalized improvement on them requires a positive density of edge changes.

Hidden-partition count fluctuations can be removed with O(beta^2 sqrt N)
optimized absolute-pressure error, or O(N^(5/4)) cap error directly. A two-bit
orientation version has exactly the desired original-cap pressure endpoints.
Its total signed drift remains unpaid. One-time concentration is not a
recurrence and cannot be summed over all reveal times.

Affine/quadratic MUB constructions have scalable, independently checked
five-vertex optimal-seed obstructions. The exact ternary packing census through
eight vertices is now independently complete. These results do not refute
transfer from selectable growing optimal seeds or arbitrary global changes.
The next work must attack genuinely dense, jointly controlled changes; do not
turn the new stationary law into another unproved sufficient condition.

Actual-cap quadratic susceptibility gives an extensive bridge gain, but at
contracted temperatures. The exact log-cosh path for weighted WIDTH minimizers
has a favorable PSD term and signed edge-flip-cost imbalance. No sublinear
integrated payment is proved; width-to-absolute recovery is a second gap.

Actual sign surgery is available under proportional quadratic ramp conditions.
General trace o(sqrt(n)), or sign-compatible trace o(n) with explicit
probability feasibility, gives vanishing normalized midpoint loss. This is
conditional. Steiner-frame full-sign examples force trace Omega(n) for every
accurate PSD ramp under bounded cap alone. They are not width minimizers.

Combining the new upper with the width lower excludes scalar-affine
Gaussian-sign mean-absolute-energy witnesses on actual original near-minimizers
by a fixed gap below width. A cap-only four-sign variance proof removes the
absolute-mean versus mean-absolute gap. This is a method restriction, not a cap bound.
No ramp-existence or original convergence conclusion follows.

## Obstructions whose scope must be retained

- Global child reversal changes bridge sign but not internal energy:
  max_epsilon |a+b+epsilon c|=|a+b|+|c|. No free bridge cancellation.
- Prescribed dense Hadamard stabilization has an actual half-floor even for
  optimal seeds. Other nonlinear/all-order constructions are not excluded.
- Generic bounded-cap integrated temperature payment is false; actual global
  minimizer payment is not falsified by those examples.
- Contracted-temperature scalar recurrences alone allow oscillating abstract
  slopes; the counterprofiles are not claimed realizable by sign matrices.
- Two-sided adaptive variance universality fails for sparse triangular arrays.
  Fixed-bounded-amplitude favorable one-sided replacement remains open.
- Strong all-high-state projection capture already forces full rank or an
  insignificant midpoint. Proportional ramps are weaker but not automatic.
- Skew doubling requires joint absolute responses. Exact finite failures do
  not establish its asymptotic Boolean impossibility; spectral floors concern
  certificates only.
- An ultrafilter LDP is not an all-order entropy limit. No imported
  random-matrix theorem currently supplies the missing realization.

## Continuing construction checkpoint: 2026-09-07 10:25 UTC

An actual all-order full-sign bridge now localizes arbitrary prescribed
subspaces of total dimension r with uniform residual bound
sqrt(n)||P_Xperp x||||P_Yperp y||+O(n^(5/4)sqrt(r)).
This is a completed construction step, not favorable flatification.
An independently verified Gaussian witness shows energy half-width at
least [4/(3pi sqrt(3))-o(1)]n^(3/2) on every near-orthogonal slice when
r=o(sqrt(n)). Separately optimized residual-norm certificates therefore
fail throughout this range; actual joint bridge control is not falsified.

Strategic correction: dephased Hadamards already control ALL fixed-center
spins with the overlap-residual inequality. Recent Gaussian-profile sector
assumptions were unnecessary for those declared energy windows. Their
proofs remain valid, but additional profile refinements are not the main
gap. Whole-child joint coverage or global internal-edge changes are unpaid.
See `artifacts/flatify_director_checkpoint_five_2026_09_07.md`.
Research continues within the current authorization.

## Closing mathematical checkpoint: 2026-09-07

See `artifacts/flatify_director_final_synthesis_2026_09_07.md` for the
six-hour campaign's reconstructed results and independent audits.
The interval is [.4333221116640807,.493608094); convergence remains open.
New actual subspace bridges allow r=o(n/log n), but a fourth-moment-based
width theorem obstructs scalar residual certificates for EVERY r=o(n).
The new tr(A^4)=O(n^3) estimate holds for arbitrary bounded-cap signings;
it does not force bounded normalized operator norm.

Most useful new tool: exact-covariance QUENCHED Gaussian-sign universality
with arbitrary deterministic child offsets. At fixed covariance gap it
pays O(n^(4/3)sqrt(log n)) expected-parent-cap error. This handles the
full configuration set, unlike the preceding annealed source bound.
The Gaussian parent cap itself has not been controlled favorably.

An actual additive-near-minimizer construction proves that the particular
opposite-spectral law normalized by ||A||op||D||op is typically iid-like
and too costly when that denominator/n diverges. This is not an all-law,
all-selection, exact-minimizer, or global-surgery impossibility theorem.
Next mathematical target: a favorable Gaussian parent comparison with
explicit admissible covariance and quantitative total cap payment for
selectable actual optimal children. No further covariance description
without that payment is counted as convergence progress.

## Precise remaining convergence obligation and next target

One sufficient target is a full-sign replacement of
B=diag(sqrt((N-1)/(m-1)) A_m, sqrt((N-1)/(n-1)) A_n),
for selectable actual minimizing children at comparable orders N=m+n,
with Q(A_N)<=Q(B)+O(N^(3/2-delta)) for a fixed delta>0.
This would yield u_N<=u_m+u_n+O(N^(1-delta)), u_n=M_n/sqrt(n-1),
and hence convergence by the verified balanced-tree argument.

This construction is OPEN, not a completed strict reduction. It allows
global old-edge changes; independent rounding and generic variance
universality do not prove it. Recommended next campaign: directly prove or
scalably falsify this fixed-oversaturation selected-child replacement.
Do not replace it with another uncharged statistic of children.

A genuine nonconvergence result must separate two infinite order subsequences
by a fixed normalized gap. No present certificate or family obstruction does so.

## Preservation and continuing discipline

Canonical proofs/certificates stay in place. Dated tracked archives preserve
unpolished research, parameters, logs, failures and subagent reports; reviewed
environments, caches, dependencies and rebuild products are excluded.
The closing publication audit records the snapshot and remaining originals.
No new theorem is considered independently verified just because a prior
agent returned PASS. See linked proof reconstructions and exact replays.
