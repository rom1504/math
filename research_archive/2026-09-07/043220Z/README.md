# Closing research preservation — 2026-09-07 04:32:20 UTC

This snapshot preserves 1,847 research-bearing files (153,117,802 bytes).
All payload hashes were verified, and the current-work audit found every
remaining research file matched, with none missing or changed. Preservation
does not validate a draft's mathematical claims.

The manifest includes ongoing scripts, parameters, informative outputs,
failed approaches and literature. Canonical proofs and scripts are staged
at their existing paths, so they need not be duplicated as working originals.
There were no detected credential candidates or oversized research outputs.
Environments/caches and reviewed build products were excluded, not deleted.

The 13 missing/dynamic path flags were manually reviewed: one is an output
example for the lower replay (the actual replay is preserved under its dated
name), eleven are build/run examples (including repeated commands), and
one is a historically removed replay. None is an undisclosed
input to the new upper, lower, ETF, sign-surgery or four-sign proofs.
The historical rp_audit.json was explicitly removed by its author; its
canonical result and script remain tracked. External example commands using
/tmp are retained as provenance, not followed by this project.

New finite checkers use standard-library arithmetic, NumPy and documented
OR-Tools where applicable. The orientation checker imports the TRACKED
decisive_bridge_actual_cavity_psd_checks_2026_09_07.py. The phase checker is
self-contained; the constant converter imports a tracked rational interval
module. Lower replay policy/certificate inputs are tracked; outputs are
preserved. These manual checks supplement, not replace, the conservative scan.

See manifest.json for original paths and SHA-256 hashes, ignored_references.json
for every scan flag, runtime.json for observed versions, and files/ for payloads.
Use research_archive/preserve.py --restore-path with this snapshot to recover
an ignored dependency without overwriting newer work.
