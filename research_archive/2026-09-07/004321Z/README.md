# Second decisive-campaign preservation checkpoint

Preservation snapshot, not a verification verdict. All 1,864 research-bearing
files identified by the inventory were copied unchanged (153,049,957 bytes).
Includes unfinished derivations, failed experiments, temporary reports and
informative outputs. Canonical proofs and programs were staged in their
existing locations before this snapshot. Claims retain their individual
proved/computational/heuristic/open status.

The substantive checkpoint proves the exact ternary Gaussian phase and the
all-order upper bound below .494515125. Its analytic dependencies and full
independent interval replay are tracked canonically. It also preserves scoped
H2 falsifiers, finite-profile transport and continuing original-limit work.

Excluded: 17,599 environment/cache files, 76 reviewed build products, 25
pinned external dependency files and eight environment symlinks. No oversized
research or credential candidates were detected. The conservative reference
scan retains 13 missing/dynamic references for review: historical absent
modules are documented by the existing preservation audit; a reference scan
does not prove dynamic dependency closure. The new phase and constant programs
use tracked Python source and the declared mpmath environment, not ignored
research data. Nothing was deleted or silently reconstructed.

See manifest.json for every path/hash and exclusion, ignored_references.json
for the reference inventory, and runtime.json for observed package versions.
