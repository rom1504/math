# Construction-track exploration record

2026-09-07, first independent hour beginning05:27UTC. This preserves failed
and unfinished research, not only polished claims. No original convergence
or favorable flatification theorem was obtained.

1. Literal H2 tensor amplification was tested before pursuing MUBs. Exact
   archived optimal children of orders3--10 already have lift/Q ratios up to4,
   rather than2sqrt(2). The omitted matching diagonal costs only O(n), but the
   finite data alone do not classify asymptotic minimizing children. Script
   and complete reproducible output are retained under the shared prefix.
2. Adaptive one-vertex insertion leads exactly to
   min_v max_x(|H_A(x)|+|v dot x|). Average local field at a maximizing spin
   is O(sqrt(n)), but exchanging the vertex choice with the maximum is unpaid.
   This route reconstructed a recorded cavity obligation; no new lemma was
   asserted. Sparse random completion still has leading full-cube cost.
3. A seed-dependent terminal version of the successful weave reconstructs the
   archived nonlocal contraction gap: product local Hilbert norms erase edge
   unitary information. No false source-domain gauge invariance was imported.
4. The adversary's convex orbit averaging observation was strengthened here
   using per-row l1 mass and Khintchine/polarization: every convex orbit average
   of comparable B remains a leading Q-distance from every full signing.
   The adversary owns the resulting independently reconstructed artifact.
   This rules out a difference-norm proof, not favorable cap cancellation.
5. Block conjugation using real MUBs genuinely retains seed spectral data and
   produces exact cross signs. Affine-net bases led to the fractional ternary
   packing functional and a scalable full-completion obstruction. Their
   within-fibre triangular payment by itself is only O(k^(-1/2)) normalized;
   it does not turn the cross lower into a full upper.
6. A tentative hope that generic high-rank Kerdock phase sums would suppress
   affine routing was tested algebraically. The explicit maximal family and
   its all-subset rank estimate are correct, but a two-dimensional adapted
   plateaued witness defeats the hoped-for lossless pentagon transfer even
   without low-rank higher sums. The exact Heisenberg covariance law pays for
   arbitrary internal completion. This is the strongest result of the hour.
7. During the plateaued search, eight real qubit directions gave pentagon Gram
   energy1+3sqrt(2)>5. This is NOT a valid real Boolean-Walsh template at even
   ambient dimension: the mixed support dimensions require sqrt(2) amplitude
   ratios, whereas bent restrictions must have even dimension. Replacing the
   qubit directions by coordinate vectors and four-coordinate flat sign
   vectors gave the exact energy5 template used in the proof. No claim based
   on the invalid sqrt(2) template is retained.
8. The high-rank finite-template embedding is a separate elementary theorem,
   suggested by the director and independently reconstructed here. Its rank
   hypothesis is unnecessary for the simpler pentagon witness. It does not
   imply that arbitrary spherical/SDP vectors are Boolean-compatible templates.
9. A proposed bounded-cap counterexample to generic partial-packing inflation
   uses mesoscopic blown-up pentagons inside a symmetric Hadamard background.
   The adversary checked the compatible H20 construction and exact constants,
   and owns that draft. The resulting cap coefficient is above2.8 and hence
   provably far from near-minimality; it must not be relabeled an optimizer
   obstruction.

No temporary file outside the repository was used. Generated JSON outputs
and all five diagnostic/replay scripts remain in their canonical directories.
The main agent owns archival copying and Git checkpoints.
