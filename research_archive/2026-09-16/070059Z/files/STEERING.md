# Strategic steering

Evidence cutoff: 2026-09-16 bounded BH-paper investigation, closing audit.
Status: authorized ONLY05:13:45--07:13:45UTC; no automatic general loop.
Original convergence remains OPEN. Mathematical judgments below are agent-owned.

## User objectives and authorization

Determine whether M_n/n^(3/2) converges, without presuming its value.
The latest authorization is a focused TWO-HOUR investigation of
arXiv2609.12427: reconstruct the paper, test powers/Walsh cancellations,
compare the older subexponential BH bound and existing project proofs,
and seek an unexpected useful mechanism. Independently verify promising
deductions; preserve and push results. Stop at two hours with an explicit
assessment; DO NOT automatically restart the general research loop.

Preserve and push substantive proofs, drafts, failed derivations, scripts,
parameters and outputs. Temporary placement must not lose knowledge.
Never use /tmp; use /home/math/quadra/tmp. Canonical proofs stay in place;
dated archives preserve other research-bearing files. Exclude reviewed
environments, caches, credentials and reproducible build products.
Write access was verified immediately; execution and normal Git push work.
Three complementary researchers and independent adversarial reconstruction
were used while the main agent derived and checked results.

These are user directives. Earlier authorizations and route history remain
in Git and ledger; suggestions and mathematical rankings are not directives.

## Reported original frontier, unchanged in this campaign

    .4333221116640807 <= liminf M_n/n^(3/2)
       <= limsup M_n/n^(3/2) <= U0-zeta < .493608094.

U0=[97/20+(24/25)log2-5151/6250]/[(97/10)sqrt(24/25)].
The selector/local-stability refinement has explicit tiny zeta>0.
No displayed-decimal change or ensemble optimality is inferred.
This paper campaign audited dependencies of its new results, not the
entire earlier lower/upper chain. The latter remains preserved in the
[September7 synthesis](artifacts/principle_director_final_synthesis_2026_09_07.md).

Upper dependencies: precision Schur, direct Gaussian stopping, certified
ternary E bound, full-spin/both-polarity counting, terminal-uniform Fock
bound, all-order realization and selector/local-stability refinement.
Lower dependencies: marked response, covariance/tree estimates, nonlinear
replacement, finite policy certificate and spectral deletion. Finite
approximations/depth precede order; remaining cutoffs are removed last.

## Agent-authored assessment: bounded paper campaign

The primary theorem/proof reconstructs correctly. A checked parameter
refinement gives weighted O(m^3) and full BH O(m^(11/2)) growth. This is
secondary analytic mathematics, not an external novelty claim or an
improvement of the original signing bound.

Strongest actual-class consequence: every Q(A)<=C n^(3/2) obeys

    max_(2<=k<=K) max(F_k,W_s(H_A^k)^(1/k))/n^(3/2)
      <= C_C[n^(-1/4)+sqrt(K/n)+K/n^(3/4)].

Thus ALL exact minimizers have vanishing adaptive power certificates for
K=o(n^(3/4)). Scalar polynomial filters have a corresponding uniform
non-detection window; eligible uniform frozen-coordinate tests do not
evade it. Explicit planted full signings make the exponent sharp for
the BROADER bounded-cap class, not for minimizers.

At linear powers an exact Renyi-entropy identity identifies the full
coefficient norm with the ordinary signed moment up to1+O(1/n).
The older subexponential BH constant already has negligible root loss.
Archived Chebyshev recovery uses the moments more efficiently, but L2
already supplies its detection; BH improves only an unnecessary overhead.
The missing LINEAR-degree optimized moment theorem is unchanged.

Other audited outcomes: scalar flattening pays a coefficient-dispersion
cost; Hilbert orbit flattening loses the scalar extreme and fails scalar
BH/influence. Norm-only joint ellipses do not improve the current seed.
In the unmasked repaired p=31/32 port model, every two-level Schur
extension either gives only o(N^(3/2)) gain or belongs to an already
small-cap sector. This does not exclude all joint packet inequalities.

Polynomial BH does improve a separate Boolean Bohr-radius rate to
relative O(loglog n/log n). Its optimizing degrees are logarithmic;
fixed quadratic degree is eventually absent from that minimum.

## Quantifiers and remaining obligations

- No claimed convergence, nonconvergence, new original bound or recurrence.
- Finite cap witnesses were replayed; solver optimality is not used here.
- Generic quadrature moment ambiguity is neither dyadic nor a realized
  quadratic signing. Its antipodal refinement does not change that scope.
- Bounded-cap planted tails are not exact-minimizer counterexamples.
- Hilbert norm/certificate failure is not an actual-signing lower bound.
- Port saturation is Euclidean and unmasked; no Boolean optimizer is
  manufactured from an eigenvector, and other joint mechanisms remain open.
- No local/spectral/tensor/algebraic-family obstruction excludes arbitrary
  global rewrites of selectable exact minimizers merely by analogy.

The prior joint-packet compiler still lacks complete many-active mixed
response control AND a separate favorable-seed-value transfer theorem.
One sufficient original theorem remains full-sign replacement of the
row-square-normalized two-child weighted block matrix with cap loss
O(N^(3/2-delta)); it is not proved and is not a mandatory architecture.

## Director decision

Do NOT extend this BH-to-signing investigation without a concrete new
signed-coefficient estimate or an actual-scalar-extremum-preserving joint
argument with a quantitative original-value consequence. Further generic
power/filter variants are not justified by the evidence here.

The analytic refinement and Bohr corollary could warrant separate external
novelty checking; that is not a reason to restart the original research loop.
Stop at the bounded endpoint. A new campaign requires user authorization.

[Paper synthesis](artifacts/bh_2026_09_16_final_synthesis.md) records exact
theorems, limitations and dependencies. [Campaign record](artifacts/bh_polynomial_campaign_2026_09_16.md)
and ledger Section10.164 record the checkpoints. Preserve proofs, drafts,
failures, sources and outputs; archive placement is not verification.
