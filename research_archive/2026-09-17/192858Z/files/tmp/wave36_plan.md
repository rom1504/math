# Wave 36 ranked ideas

Boundary evidence: README, Wave 35 Updated frontier, and STEERING.md were
reread after commit 44e2b58.  The leading route remains optimized principal
restriction; no claim below is a user directive.

1. **Agreement-to-row transfer at the actual cut scale.**  Starting from the
   singleton-anchored conflict decoder, use the exact selector-sampling
   identity for local row squares, local optimality of favorable labels, and
   Hamming robustness to prove the weaker `R_2=O(n^(9/4-c'))` bound needed by
   (10.795), rather than the stronger `2n(n-1)` center cap.  If false, seek a
   complete-signing or sharp abstract counterexample with all hypotheses.
2. **Slack-aware simultaneous replacement.**  Attack (10.1007) (or its
   row-free precursor) directly by coupling all selected blocks and charging
   anchored conflict at `kappa=4(n-1)`.  Any proof must include favorable
   support, survive the A9 zero-slack witness-migration wall, and quantify
   project-scale slack.
3. **Direct entropic hinge through a row-good compressed response.**  Combine
   (10.985) with the block-coset row theorem and a nonlinear positive-part
   response inequality for `H_w`; seek one tilt of entropy `O(TL0)`, not a
   for-all-law learner.  Audit whether quadratic norm response can dominate
   the restriction excess without assuming the circular uniform mean lemma.
4. **Selector-information cancellation theorem.**  Use (10.1010), fixed-size
   selector support, and invariance of components omitting a vertex to bound
   the erasure sum `sum C_j` directly, retaining the global orientation.
5. **Bounded-backtracking theorem or falsifier.**  Analyze exponential-family
   context masses in (10.1019); prove a uniform relative backward-variation
   charge under exact-minimizer structure, or construct a scalable/finite
   counterexample that identifies an additional necessary hypothesis.
6. **Mismatch-code compression from shared child fibers.**  Replace the
   exponential arbitrary mismatch partition in (10.996) by a compression or
   VC bound specific to favorable labels of one exact minimizer.  Do not infer
   this from the Euler system alone.
7. **Tiny-soundness complete-complex agreement with shrinking error.**  Check
   whether Z/V-test reverse-hypercontractive theorems can improve the known
   `O(TL0)` error to `d=o(TL0)` for our list-valued fibers.  Current primary
   results suggest a parameter mismatch, so this is a falsification-oriented
   literature route.
8. **Exact-child completion collision channel.**  Since every completion of
   an exact child ground is loss-favorable, choose outside spins by a
   row-aware channel and seek an `e^{-O(TL0)}` common output through reverse
   hypercontractivity, without reverting to the circular mean-entropy lemma.
9. **Free-coordinate terminal-min-cut screening.**  Build a random Steiner
   tree whose cost is coordinate conflict paths rather than Johnson entry;
   calculate whether exact-minimizer overlap can give `O(k0)` cost at the
   required uniform probability.
10. **Deletion-field fallback.**  Revisit constant shortfall only through a
    new exact order-statistic theorem for locally stable ground fields that
    yields the required square-root deletion reward; do not restart the
    pressure criterion without such a mechanism.

Selected parallel attacks: 1, 2, and the combined 4--5 harmonic package.
The main agent attacks 3 independently.  These are independent in their
decoding, replacement, harmonic, and direct-cut inputs.
