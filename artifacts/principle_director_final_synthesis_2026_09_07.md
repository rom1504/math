# Synthesis-to-principle campaign: rigorous outcome and remaining gap

Campaign started2026-09-07 at15:24:01UTC; authorized endpoint21:24UTC.
This synthesis is being assembled while final proof audits continue.
It is not evidence of an earlier stop. Canonical proofs and all exploratory
work are preserved separately, with their own verification status.

## 1. Original problem: not solved

The proved original frontier is

    .4333221116640807 <= liminf M_n/n^(3/2)
       <= limsup M_n/n^(3/2) <= U0-zeta < .493608094,

where U0=[97/20+(24/25)log2-5151/6250]/[(97/10)sqrt(24/25)] and
zeta>0 is explicit but extraordinarily small. The new strictness below
U0 comes from actual selector uncertainty and local stability, not from
assuming the construction is optimal. No displayed decimal improvement
from this tiny zeta is advertised. Convergence and nonconvergence both
remain unproved; no two asymptotically separated order subsequences arose.

The original70-digit E certificate and the independent60-digit anti-weave
certificate were replayed. Their analytic dependencies, physical spin
counts, both polarities and all-order limit order were reconstructed.
Finite caps and solver timeouts were not used as asymptotic proof steps.

## 2. A positive principle, not just a taxonomy

The most useful new principle is JOINT RARE-PACKET ACCOUNTING:

    Retain the physical event that creates several response channels.
    Pay that event's entropy once, then use an exact mixing inequality
    to charge the linked channel responses together.

This is now a proved construction mechanism. It is not an assertion that
every bounded summary preserves the maximum, nor another definition whose
only theorem assumes convergence of its state.

The general information theorem is as follows. If X_r is within O(r) of
a fixed finite alphabet {0,b_1,...,b_d}, with nonzero mass O(r), then for
t=tau log(1/r), uniformly over all atom probabilities,

    E_t(X_r)=-log(1/r) sum_j p_j min(tau b_j^2,1)
                                             +o(r log(1/r)).

E_t is the original conditional-variance envelope. The proof minimizes
over ALL latent channels: information charges separated rare atoms,
while variance charges unseparated ones. An explicit reveal/pool channel
matches the bound. It is not a chosen-channel numerical approximation.

For ANY dephased Hadamard H_L and nonempty proper column subset S,
C_j=sum_(l in S)H_jl satisfies the exact inequality

    sum_j min(C_j^2/(L-1),1) >= 1+|S|/(L-1).

The entropy of a rare physical packet is charged once; this inequality
then forces enough total response cost over its linked outputs. Its
linearity in |S| makes it uniform over arbitrary packet mixtures.

## 3. The actual construction consequence

Use a Sylvester node L32, retain exactly31 coordinates in every node
group, dephase a marked constant column, and attach independent deep
ordinary/marked child bases. Repair columns to exact sign balance with
operator error O(m^(1/4)log^(5/4)m). This produces actual rank-one sign
blocks, not a fractional matrix renamed a signing.

For its balanced bulk B, N=mq, p=q/m->31/32, one can choose the SAME
construction with the following verified response controls:

- On the all-fibre-balanced Boolean face, Q_bal(B)/N^(3/2)<.497761196.
  This includes the selector entropy fee and an independently replayed
  directed certificate E_(97/20)(nu_(31/32))<=-4/5.
- On near-constant density bands, the limiting variance coefficient is
  sqrt(31/32)/2=.492125492125738..., strictly below1/2. Every fibre may
  be constant or have either majority polarity. A common temperature
  handles minority scales from r to nearly r^2 at this best coefficient;
  at coefficient .499 it also handles the full band [r^3,r].
- A positive mixed-profile cone follows at the SAME fixed temperature:
  balanced rows have a negative exponent margin that pays the declared
  entropy/variance costs of the other rows. For example, at least1/400
  balanced fibres and all other minority fractions<=10^(-6) give a
  certified .499 variance bound, even with arbitrarily many finer scales.

These statements include uniform additive o(N^(3/2)) errors, not relative
control at microscopic variance. Fix the band and margins, then a finite
depth, then take order to infinity; shrinking margins uses a diagonal
choice afterward. The physical diagonal fibres are explicitly separate.
These bulk statements do NOT assert a new full-sign minimax constant.

An independent actual fair-edge stability theorem excludes near-constant
ferromagnetic-completion local maxima unless their minority mass is
predominantly in coherent incoming tails or nearly balanced fibres.
It uses exact flip inequalities, convex concentration and graph Finner,
not a Gaussian surrogate. A separate selector-conditioning construction
controls all biases when only o(m/log^4 m) fibres are nonconstant, with
the RELATIVE coefficient sqrt(p)/2+o(1), even at microscopic variance.
It preserves every strict prior row certificate: the conditional selector
density is uniformly bounded, costing only O(1) per physical row. The
primary Rudelson--Vershynin deterministic sparse Rademacher lemma is used
with independent NONIDENTICALLY distributed missing rows and exact
aggregate isotropy. Root and both auditors reconstructed this adaptation;
conditional child independence is neither asserted nor needed.
These are complementary regions, not a claimed covering of all words.

Proof and director reconstruction:
`principle_invent_2026_09_07_stratified_marked_selector.md`,
`principle_director_joint_packet_audit_2026_09_07.md`,
`principle_construct_2026_09_07_balanced_entropy_mixed_cone.md`, and
`principle_synthesis_2026_09_07_balanced_bulk_sparse_stability.md`.

## 4. What the audits corrected

The unstratified scalar envelope has an EXACT optimized rare-bias floor
1/(2sqrt(p))>1/2. The new packet mechanism genuinely escapes that
certificate failure. It does not do so by tuning its temperature again.

Exact balancing and a strict balanced-face cap alone are insufficient:
macroscopic four-edge balanced switches create actual biased scars with
coefficient>1/2 while preserving both endpoint properties. The new theorem
uses the construction law and joint packet count, not those endpoints alone.

The tempting H24 same-law inheritance was not proved merely by finding a
transitive unsigned automorphism. Its output permutations mix different
child laws, and convexity of E does not justify that mixture step. The
safe H32 translations preserve each fixed output law up to its permitted
sign, resolving the issue exactly. H24 certificates and automorphisms are
preserved with the missing inheritance step explicitly labeled.

Naively iterating the packet construction is also not a solution. Two
32-way tensor stages admit singleton and product-half packets requiring
incompatible temperatures. A separately specified DC-spine iteration has
its own finite bandwidth obstruction. These are rigorous envelope-policy
falsifiers, NOT impossibility theorems for actual favorable signings or
every adaptive hierarchy.

## 5. Other theorem-level consequences of the synthesis

The strongest new statement applying directly to actual LIMINF families
comes from hierarchy-aware sign augmentation. Let e(eta) be the limsup
normalized gamma2-square of their FULL absolute eta-near-level set. Then

    limsup_(eta->0) e(eta)h(min(e(eta),1/2))/eta
                                      >=3 c_inf/(40 phi^5).

This follows from an actual old-edge-preserving construction: below that
threshold it inserts epsilon n signed vertices at incremental slope less
than the normalization's3c_inf/2 and contradicts liminf optimality.
It is stronger than a generic thick-face observation. It does not prove
that near-minimizers have a compressible phase code or an all-order limit.

Independent-port pressure and minimum defect are now universal for ALL
row-energy-bounded profiles when Q=o(m^2/log m), including every bounded-
cap selectable exact seed. An actual logarithmic multiscale example proves
that this threshold cannot be weakened to Q=o(m^2). Zero-temperature and
diverging-temperature extensions are also proved. The quantifier is
QUERYWISE placement; it does not commute with the worst-query maximum.

Every fixed-size independent directed-frame law has an actual cap floor
above.50717387, including the specified exact orthogonal-frame repairs.
Rare or globally correlated frames are not covered. Other old local,
tensor, algebraic and spectral no-gos likewise remain within their
proved laws and quantifiers; none was generalized to all rewritten
selectable exact parents without proof.

## 6. Exact remaining obligations and research judgment

The construction now has actual mixed-response regions, not merely an
unproved covariance target. What it still lacks is a joint bound for the
intermediate/coherent multiscale region outside the proved bands and cone.
Even closing that model-specific bound would NOT identify the original
asymptotic optimum: favorable-seed value preservation remains separate.

One sufficient original-value theorem is still: for comparable orders and
selectable exact minimizing children, globally replace the row-square-
normalized weighted block matrix by full signs with extra cap
O(N^(3/2-delta)), uniformly all comparable large orders. Dividing by
sqrt(N-1) gives the documented summable-defect almost-subadditivity.
This theorem is NOT proved here, nor is the new construction claimed
asymptotically optimal. A direct variational characterization could
replace it; no preference is a mathematical premise.

Judgment: the synthesis yielded a usable positive principle and a new
actual biased-response theorem, not convergence. The most defensible next
campaign is to attack the ACTUAL intermediate/coherent mixed response of
the joint-packet construction, with a proof/disproof team and the complete
parent cap as the metric. Require a uniform quantitative parent inequality,
not another envelope variant; retain original seed transfer as an explicit
additional gap, rather than quietly counting it as solved.
