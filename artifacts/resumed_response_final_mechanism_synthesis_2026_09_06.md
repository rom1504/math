# Response track: final mechanism map and proved limits

Date: 2026-09-06, checkpoint updated 07:10 UTC. This is a navigation/scope note,
not a replacement for the proofs or independent audits. The original
convergence/nonconvergence problem remains open.

## 1. Banked original-class lower bound

The exact, independently byte-replayed bound is

    liminf M_n/n^(3/2) >= .4333221116640807.

It is obtained by one actual rich-core Gaussian response birth followed
by a two-dimensional policy improvement, with exact true inverse norms,
correlated-mask conditional Jensen, and an explicit omitted-Hermite tail
charge. The full proof and frozen inputs are in
`resumed_response_rich_core_response_birth_2026_09_06.md`, Section 7,
and `resumed_bound_audit_rich_core_cell_ascent_2026_09_06.md`.
The exact result is
`computations/results/resumed_response_rich_core_optimized_policy_certificate_2026_09_06.json`.

No mixed-charge numerical experiment changes that universal lower bound.

## 2. A sharp scope boundary, not a bound on actual signings

The entire single-terminal Gaussian functional satisfies J<.45. The
analytic reduction is in
`resumed_response_terminal_functional_ceiling_and_scope_2026_09_06.md`;
the independent rational envelope certificate is in
`resumed_bound_audit_full_response_uniform_upper_2026_09_06.md`.
Its uniform upper endpoint is .44999892749193173697... .

For fixed old-tree terminal algorithms on normalized Hadamards and the
stated stable perturbations, BOTH discarded common self-energies vanish.
Their actual endpoint energies converge to +/-J, not to J plus a hidden
constant. The proof is
`resumed_response_flat_involution_terminal_endpoint_2026_09_06.md`,
independently audited in
`resumed_bound_audit_flat_endpoint_primary_2026_09_06.md`.
Thus more masks, frames, or births that are finally reduced back to this
one functional cannot overcome .45 on that matrix class.

This is neither an upper bound on the original Boolean optimum nor on
all finite feedback algorithms. General fixed GFOMs have the separate,
weaker Haar comparator ceiling sqrt(15)/8=.4841229182759... .

## 3. Mixed-charge feedback is an actual new mechanism

The physical update u_next=sign(Bu+alpha u) feeds back the WHOLE previous
endpoint. Its finite-depth law is given by exact conditional Haar
query/response pairs: the unexplored residual produces a fresh Gaussian,
and the known span is acted on by a swap. See
`resumed_response_mixed_charge_involution_feedback_2026_09_06.md` and
the independent proof
`resumed_convergence_haar_adaptive_query_kernel_2026_09_06.md`.

Already at two updates, its charge-odd half D_2 is not measurable in the
complete old canonical Gaussian frame. At alpha_0=alpha_1=1/2, the exact
squared L2 distance from that whole frame is strictly greater than
1/102400. The proof is a conditional Bernoulli variance and a simple
Gaussian rectangle; the independently byte-replayed exact certificate is
`computations/results/resumed_response_mixed_charge_distance_certificate_2026_09_06.json`.
The new information is a response fluctuation correlated with the
already observed field. Replacing it by an unrelated old even gate with
the same marginal distribution destroys precisely that correlation.

One frozen 40-step inertia schedule has floating state-evolution energy
about .45162. An exact integer replay at n=2^18 has energy
30324543/67108864=.4518709033727646... . These are, respectively, a
numerical asymptotic discriminator and an exact finite example. They
are NOT a certified asymptotic crossing of .45, and neither is a
universal original-class bound. Sources/results are linked in the main
mixed-charge proof. No parameter optimization is left running.

## 4. What genuinely iterates, and why the increments need not stay large

After any nondegenerate Boolean query with limiting field k+sigma N,
the actual stability gap g=E|k+sigma N|-E[u k] is strictly positive.
The feasible damped endpoint (1-g/4)u+(g/4)sign(Bu) improves actual energy
by at least g^2/8 in the population limit; finite approximants retain a
strict margin. Every fixed finite inertial-sign iterate is nondegenerate.

More generally, every finite causal paired history has a compact moment
body for actual energy c^T Jc/2. At a maximizer ||c||<1: a largest-index
fresh-Gaussian argument rules out a bounded norm-one stationary vector.
Compactness gives a positive frame-dependent residual floor. One actual
new query plus feedback therefore strictly exceeds the best population
energy of that entire finite history. This theorem, including measurable
approximation margins and redundant-query deletion, is in
`resumed_response_paired_history_actual_energy_extension_2026_09_06.md`.
The director and bound-audit agent independently reconstructed it.

Consequently the supremum over all fixed finite equivariant GFOMs on
the flat-involution class is not attained by any finite algorithm. This
does not identify that supremum or the original-signing limit.

There cannot be a positive depth-uniform stability gap in this broad
feedback class: the independent auditor constructs fresh-gated damped
Boolean updates with arbitrarily small positive gaps, while their
energies remain below the fixed-GFOM ceiling and hence a fixed distance
below 1/2. See
`resumed_bound_audit_mixed_charge_feedback_2026_09_06.md`, Section 5.
Thus strict extension is real, but a uniform rate or sharp endpoint
requires additional structure not supplied by feasibility or local
stationarity alone.

## 5. Essential open comparison

The mixed-charge kernel and the actual-energy iteration theorems are
proved for Haar/flat involutions and explicit stable comparators. The
original minimization ranges over ALL hollow sign matrices. No argument
here proves that its minimizing sequences are flat, involutory, or
universality-comparable. That missing comparison, together with the
unidentified feedback optimum and possible growing-depth effects,
prevents these mechanism results from resolving the original normalized
convergence question.

## 6. Final original-class comparison audit

For arbitrary bounded-op hollow signings, some terms ARE identified:

    E Q_B(F_old)/n = .5 ||P1F||^2 Tr(B^3)/n+o(1),
    E Q_B(S H_old)/n=o(1),
    B[S H_old] is the old creation field UH in ordered normalized L2.

This gives the genuine conditional original-matrix lower bound
|E H U^*F|+.5||P1F||^2 |Tr(B^3)/n|. The independent proof/audit is in
`resumed_response_original_bounded_op_cubic_and_transport_audit_2026_09_06.md`.
It cannot improve the universal constant when the cubic moment vanishes,
and a concrete hollow twin-Hadamard example shows that vanishing cubic
moment does not imply involution structure.

The exact return is BF=B^2[S U^*F]+Z_R, not S U^*F+Z_R. At the next
mixed step the uncontrolled object is a cross-root transport of
nonlinear functions of this already exposed field. Equivalently it is
the new term B[H sign(BF)] after a terminal best response. Existing
restricted tests enter BF only linearly and do not close that new
operation. This is a precise missing identity, not a blanket assertion
that a universal mixed-charge gain is impossible.

There is also a fully generic actual gain for a fractional mean u with
average slack d>=d_0>0. New independent Boolean rounding produces row
noise variance d+O(1/n) on every normalized hollow signing. A direct
smooth-Lindeberg argument and damped best response yield gain
Gamma_sqrt(d_0)(L sqrt(1-d_0))^2/(8L)>0. See
`resumed_response_original_fractional_rounding_gain_2026_09_06.md`.
The current pure ternary/binary terminal policy has exactly Boolean
endpoints and zero slack. Deliberate damping incurs a larger energy
cost than this conservative Gaussian-only guarantee can repay. Thus
this valid generic lemma does not increase the banked constant.

A final, stronger finite ORIGINAL-CLASS theorem replaces the small
independent-noise guarantee by correlated Gaussian rounding with
covariance I+/-B/L. It preserves all coordinate means and proves

    Lambda(B)>=|e_B(u)|+d^2/(4pi L)
                  -1/[pi L(n-1)]-1/[2L^2 sqrt(n-1)].

Thus every epsilon-near-optimal fractional mean has
d<=sqrt(4pi L(epsilon+the displayed error)). This quantitative endpoint
rigidity and polynomial slack gain were independently reconstructed by
the director, response agent, and bound auditor. Full proof:
`resumed_response_original_correlated_rounding_slack_2026_09_06.md`.
The successful pure endpoint still has zero slack, so this does not
change the universal lower constant. Exact matrix-covariance sampling
is not asserted to be a free single-query operation.

## 7. Uniform finite-query refinement

The independent auditor strengthened the fixed-frame escape to a
depth-only theorem, with an explicit (very small) coefficient-peeling
modulus. At a history of at most m causal query pairs, the exposed-span
deficit and the old-history gradient gap cannot both be arbitrarily
small. Either an old-history move improves actual energy, or genuine
fractional innovation permits one new query and feedback to improve it.
Thus the optimal fixed-query values obey C_(m+1)>=C_m+Delta_m with
Delta_m>0 depending only on m. This does not provide a depth-uniform
increment. Full proof and explicit modulus:
`resumed_bound_audit_uniform_query_depth_hierarchy_2026_09_06.md`.
The response agent independently reconstructed both the peeling and the
explicit threshold algebra.
