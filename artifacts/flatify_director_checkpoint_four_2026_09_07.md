# Fourth checkpoint: actual center-conditioned bridges and their missing exceptions

2026-09-07, approximately 09:40 UTC. Research continues through 11:28 UTC.
The original interval remains [.4333221116640807,.493608094). No convergence
or nonconvergence theorem is claimed.

## Strongest actual construction step

The rank-two rectangular sign ensemble now controls a joint two-replica
sector around ANY prescribed physical child centers. The center's actual
Hadamard coefficient word is retained; it need not be Gaussian or flat.
The candidate reference is (Z,alpha Z+sqrt(1-alpha^2)G). A Schur complement
identity integrates only G and makes the compensated moment pointwise
constant in Z. Exact transport and conditioning recover the actual sign
ensemble rather than replacing it by a Haar matrix.

At alpha=4/5 the rational certificate controls bridge magnitude .74 n^(3/2)
throughout the declared sector, with the common center value o(n^(3/2)).
Its rate log(108/25)/2 exceeds the full pair-cloud entropy 2h(.1). The proof,
matrix identities and rational certificate were independently reconstructed
and replayed. See joint_noisy_center_bridge_sector and joint_gaussian_bridge_audit.

The independent verifier subsequently certified the full common-noise-level
interval at seed cap c>=.47, using 522 directed rational cells plus spectral
endpoint bounds. Padding into dense H2/H12 orders and restricting loses
o(n^(3/2)) uniformly, so the SECTOR operation has all-order realizations.
This is not a whole-parent estimate. Actual iid noisy child extrema enter
the sector with probability tending to one, uniformly in the centers.
No statement that typicality controls all states is made.

## What the actual-child calculation rules out

The half-range lower bound supplies actual same-polarity noisy shells of
entropy at least 2h(.1) on every pair of optimizing children, irrespective
of relative orientation. Their entropy exceeds the best fixed-pair
regular-support Chernoff rate by more than .03 at the flatification budget.
Thus even exact shell counts cannot repair that unconditioned certificate.
This is a certificate obstruction, not impossibility of correlated bridges.

A new actual rank-two exceptional-cloud counterexample and its scalable
inflation have been independently reconstructed and replayed by both root
and verifier. The center bridge energy is exactly zero while targets at
Hamming fraction tending to .1 have bridge value at least
189/(160 sqrt(2))>.83. Exact sphere averaging aligns ACTUAL optimal children
with these targets, producing parent excess greater than .07 n^(3/2)
over 2sqrt(2)M_n. The quantifiers are existential in the bad bridge and
child alignment. This does not disprove selection of a favorable bridge
for fixed children, nor flatification with global internal-edge changes.
See scalable_exceptional_cloud and exceptional_cloud_audit.

## Secondary rigorous structure, with scope retained

* Exact plateaued Hadamard rows have deterministic cross cap at most
  sqrt(2)/3, with a quantitative extension when some rows are unrestricted.
* Gaussian, noisy-Gaussian, and heterogeneous erasure-posterior profile
  sectors have actual full-spin certificates. The Gaussian endpoint is
  exactly neutral in the four-coordinate kernel; mixture labels must be
  independent per coordinate. A proposed shared-label extension was false.
* Independently randomized recursive Hadamard frames suppress the expected
  exact-flat codebook to exp(o(k)); sparse-support counting also gives a
  strict entropy bound for integral spectra. This does not control the
  fine, nonintegral profiles or transfer arbitrary seeds.
* The direct quadratic kernel and bare joint transport potential fail to
  be closed under one Boolean recursion step. Those exact failures concern
  the certificates, not the actual sign construction.

## Precise gap and continuing priorities

The missing original theorem is still full-sign replacement of selectable
row-normalized optimal children with O(N^(3/2-delta)) extra cap. The current
center-conditioned construction pays a real joint-cancellation obligation
on a large sector, but not the exceptional pairs, arbitrary energy windows,
or a covering by multiple centers. None of those unpaid quantities is
counted as a smaller convergence obligation. Continue attacking whether
actual optimizer information can pay those exceptions; audit the new
scalable falsifier in parallel.

## Preservation

All drafts, failed tests and interval INCOMPLETE outputs are preserved.
Snapshot `research_archive/2026-09-07/093231Z` contains 1,905 payloads,
176,506,678 bytes; no secret-pattern candidate or unhandled oversized file.
The same 14 historical/dynamic dependency flags were reviewed previously.
The 176,489,665-byte order-2048 witness JSON is retained losslessly as a
tracked gzip (approximately 1.6 MiB), with compressed and uncompressed
SHA-256 hashes and bytewise decompression verification. Its original is
not deleted. These are heuristic witnesses, not cap upper certificates.
