# Twisted chiral double: bounded campaign

Authorization: 2026-09-18 17:09:11--20:09:11 UTC. The user explicitly
reopens the original signing-convergence problem for this experiment only.
Workspace write access is verified by this tracked edit. Existing mathematical
assessments remain agent-authored; the external finite-order hint is unverified.

## Exact target and evidence discipline

For hollow symmetric sign A of order n, B=P^T D_s A D_s P and diagonal sign d,
study D=[[A,B+diag(d)],[B+diag(d),-A]]. Use Q=max|H| with H=sum_(i<j)a_ij x_i x_j.
Older chiral documents sometimes use max|x^T A x|=2Q: translate explicitly.
Seek an actual uniform construction estimate, not chiral centering alone.
Certified witness caps, optimized family values, and global M_n certificates
will be reported separately. All computations must retain inputs and seeds.

## Opening agent-authored proof obligations

1. Independently reconstruct the exact cut/internal-energy formula.
2. Search switched/permuted doubles of stored exact children and other seeds.
3. Quantify simultaneous, same-configuration control of the two summands.
4. Prove what any proposed error estimate implies for all orders; a dyadic
   subsequence by itself does not settle convergence.
5. Audit the shell-preserving null-space proposal, including escaping states.

No new uniform inequality or finite-family optimality is asserted at opening.

## First substantive checkpoint, 17:37 UTC

Verified algebra: both the supplied cut identity and
Q(D)=max_(x,y)(|H_A(x)-H_A(y)|+|x^T(B+diag d)y|) hold exactly.
The matching changes cap by at most n. Independent shell audit proves
full rank from zero/single/double flips and supplies an exact outside-shell
escape example; approximate local preservation is not overinterpreted.

Exact finite computations: fixed optimal-child families at orders3..6 attain
parent caps5,10,13,18. Three order7 classes have family minima21,21,25;
both order8 classes have minimum30. Existing global lower certificates are
imported where stated, not silently re-proved. A new full root-gauged order9
census reproduces M9=12 and finds nine minimizing classes; its proof program
is undergoing independent audit. The director independently reconstructs
stored parent witnesses4..16 by signed chiral antiautomorphisms.

Order18 witness33 is independently checked over the entire262144-spin cube.
The current order20 witness44 is only an upper bound; other optimal children
and exact fixed-child searches remain active.

Uniform negative theorem: every twist has Q(D)>=gamma(A)>=beta(A)-n,
where beta is the Boolean bilinear norm and gamma optimizes its diagonal.
A planted Sylvester construction proves that arbitrary bounded normalized
cap does not imply the desired2sqrt2 transfer. The strengthened construction
is independently reconstructed in the adversary artifact and does NOT reach
the near-minimizer regime. No arbitrary-family obstruction is promoted to an
obstruction on selectable exact minimizers.

Positive theorem status: an exact marginal-profile first-moment selection
criterion is proved but is too weak on tested seeds. Doubling with a summable
defect requires a further order-coverage theorem; a selected thinning law or
second independent multiplier would suffice. None is proved for these doubles.

Research continues after this checkpoint. Main next obligation is the actual
joint same-pair upper estimate for favorable minimizing seeds, not bookkeeping
or a change of notation. The computational track is broadening seed coverage;
the adversary is testing all chiral order16 matrices as a relaxation.

## Interruption and resumption

A service usage limit interrupted the agents on September18 after roughly
50 active minutes. Existing bounded computations completed without further
agent reasoning. The user's repeated "Continue" messages resume the same
campaign on September19 at18:58:29UTC. Approximately2h10 of active work
remains, with the assessment due21:08:29UTC. No idle interruption is counted
as research. Filesystem write/execution access is verified by this tracked
edit and the resumed audit commands; the previous checkpoint81a6073 was
successfully pushed before the interruption.

On resumption, both exhaustive optimal-child order10 lift searches have
completed with minimum44. An independent integer census has also completed
all2^36 root-gauged child signings and reproduces exactly the same two
minimizer classes. A nonoptimal conference child of cap15 has an untwisted
order20 double of cap40, independently checked by Gray and Walsh evaluation.
Thus exact child minimization does not minimize the parent cap, already at
order20. This is a finite quantifier counterexample, not an asymptotic
obstruction to a lift with an O(n) defect.

## Resumed checkpoint, September19 19:30UTC

Complete finite audit: nine optimal order9 classes have parent-family minima
33 or37; both optimal order10 classes have minimum44. A nonoptimal conference
order10 child gives40. Independent census arithmetic, canonical twist weights,
and all-spin witness checks are preserved separately. Global optima beyond14
are not asserted. An elementary symmetric-Hadamard argument independently
certifies the40 upper bound and explains why its tensor square loses the gain.

Uniform positive progress: near-order restriction retains the exact twisted
orbit relation with O_K(n sqrt(r)) error for r=o(n), with no spectral assumption.
The proof has an independent complete audit and105 exact finite checks.
A nonlinear joint diamond inequality costs4Q, not separately paid scalar
channels; its possible low-cap improvement remains under investigation.

No2sqrt2 uniform transfer or all-order convergence theorem has been proved.
The joint nonoptimal-seed search at20 continues for its single fixed hour;
failure to improve40 will not be reported as a global lower certificate.

## Resumed checkpoint, September19 20:10UTC

The joint diamond inequality is sharpened to Delta(A)<=4beta(A)/3 with a
14-term rational matrix certificate, independently reconstructed entrywise.
A weighted witness proves sharpness of this fractional resource. Its exact
physical implementation uses balanced clones; it is not a native twist.
The resource-to-native gap is leading-order on conference sequences, using
the reported original lower theorem. No original bound improves.

The director proves an exact native identity Q(D)=max_J beta(C_J) using
coordinate-pair quarter rotations. It has a full independent audit, but
retains exponential cut/bilinear optimization and is not a strict reduction.
Switch-only twists are exactly untwisted doubles of partial seed reversals;
full permutations materially improve several finite optimal-child families.

The first joint search has completed its3600-second budget:4,139,221 exact
evaluations, best40, two inequivalent parent witnesses. The second witness
cannot be made Hadamard by any choice of diagonal. These are exact witness
facts, not a family-wide/global lower certificate. One final1200-second run
widens the permitted child cap from17 to23; no additional extension is planned.

The remaining constructive diagnostic is whether the native optimized cap
F(A) can obey4beta(A)/3 with subleading error. Finite6 already falsifies its
zero-error version; coarse block rounding only proves an n²-scale statement.
The main2sqrt2 near-minimizer doubling and all-order coverage obligations
remain unchanged. Work continues until the bounded endpoint21:08:29UTC.

## Closing assessment, September19

The completed research and independent audits are synthesized in
`twisted_chiral_final_synthesis_2026_09_19.md`. The strongest constructive
result is two-sided near-order transfer inside the actual twisted family,
with O(n sqrt(r)) error. It does not supply a fixed-ratio recurrence.
The exact stability-permanent selector was tested against complete small
averages and fails to recover their known family optima. Explicit low-cap
conditional examples show why a pointwise stability penalty is unavailable.
The new tensor witness quantifies the loss of both finite cap40 parents
under literal self-tensoring. Neither upper bounds nor convergence improve.
No further research loop is authorized after this three-active-hour endpoint.

Closing publication: all three researchers have finished, no research process
remains running, and the main theorem/certificate/archive commit8ccc43f was
pushed. Final scope corrections and replay metadata are committed separately.
The post-publication archive audit found no missing research. The bounded
campaign is complete; convergence remains unresolved.
