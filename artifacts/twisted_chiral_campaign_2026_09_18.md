# Twisted chiral double: bounded campaign

Authorization: 2026-09-18 17:09:11--20:09:11 UTC. The user explicitly
reopens the original signing-convergence problem for this experiment only.
Workspace write access is verified by this tracked edit. Existing mathematical
assessments remain agent-authored; the external finite-order hint is unverified.

## Exact target and evidence discipline

For hollow symmetric sign A of order n, B=P^T D_s A D_s P and diagonal sign d,
study D=[[A,B+diag(d)],[B+diag(d),-A]]. Use Q=max|H| with H=sum_(i<j)a_ij x_i x_j.
Older chiral documents sometimes use max|x^T A x|=2Q: translate explicitly.
Seek an actual uniform construction estimate, not chiral centering alone.
Certified witness caps, optimized family values, and global M_n certificates
will be reported separately. All computations must retain inputs and seeds.

## Opening agent-authored proof obligations

1. Independently reconstruct the exact cut/internal-energy formula.
2. Search switched/permuted doubles of stored exact children and other seeds.
3. Quantify simultaneous, same-configuration control of the two summands.
4. Prove what any proposed error estimate implies for all orders; a dyadic
   subsequence by itself does not settle convergence.
5. Audit the shell-preserving null-space proposal, including escaping states.

No new uniform inequality or finite-family optimality is asserted at opening.

## First substantive checkpoint, 17:37 UTC

Verified algebra: both the supplied cut identity and
Q(D)=max_(x,y)(|H_A(x)-H_A(y)|+|x^T(B+diag d)y|) hold exactly.
The matching changes cap by at most n. Independent shell audit proves
full rank from zero/single/double flips and supplies an exact outside-shell
escape example; approximate local preservation is not overinterpreted.

Exact finite computations: fixed optimal-child families at orders3..6 attain
parent caps5,10,13,18. Three order7 classes have family minima21,21,25;
both order8 classes have minimum30. Existing global lower certificates are
imported where stated, not silently re-proved. A new full root-gauged order9
census reproduces M9=12 and finds nine minimizing classes; its proof program
is undergoing independent audit. The director independently reconstructs
stored parent witnesses4..16 by signed chiral antiautomorphisms.

Order18 witness33 is independently checked over the entire262144-spin cube.
The current order20 witness44 is only an upper bound; other optimal children
and exact fixed-child searches remain active.

Uniform negative theorem: every twist has Q(D)>=gamma(A)>=beta(A)-n,
where beta is the Boolean bilinear norm and gamma optimizes its diagonal.
A planted Sylvester construction proves that arbitrary bounded normalized
cap does not imply the desired2sqrt2 transfer. The strengthened construction
is independently reconstructed in the adversary artifact and does NOT reach
the near-minimizer regime. No arbitrary-family obstruction is promoted to an
obstruction on selectable exact minimizers.

Positive theorem status: an exact marginal-profile first-moment selection
criterion is proved but is too weak on tested seeds. Doubling with a summable
defect requires a further order-coverage theorem; a selected thinning law or
second independent multiplier would suffice. None is proved for these doubles.

Research continues after this checkpoint. Main next obligation is the actual
joint same-pair upper estimate for favorable minimizing seeds, not bookkeeping
or a change of notation. The computational track is broadening seed coverage;
the adversary is testing all chiral order16 matrices as a relaxation.
