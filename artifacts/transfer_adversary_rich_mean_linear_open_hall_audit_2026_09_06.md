# Independent audit: fixed-source rich mean-linear open Hall bound

2026-09-06, 22:07 UTC. PASS at the explicitly finite polynomial-source
scope of `transfer_seed_rich_mean_linear_open_hall_2026_09_06.md`.
Read that entire note and Sections 1--3 of its exact open-mark
dependency, `continued_audit_next_return_open_mark_hall_2026_09_06.md`.
The graph-merge and exact equality-pattern hypotheses of that dependency
remain in force; this is not a theorem about arbitrary tensors having
only a local central limit law.

For k disjoint left noise branches, each with P>M distinct matched
labels, any k' selected branches have at least P k' distinct labels
incident to right primitives. A right primitive has at most M slots.
Consequently their neighbor set has size at least P k'/M>k', giving
the required Hall matching into distinct right primitive vertices.
Open right marks when d>kP do not use up matching capacity: they are
retained tensor coordinates, not additional scalar summations.

The selected left/right vertex pairs are disjoint. Since P>M, no
single matched primitive can consume a whole left branch without a
retained mark. The proper-cut or fixed-slot cases of the exact
open-mark merge lemma therefore each supply one epsilon gain; the
old equal-degree exceptional primitive case is absent. Exact internal
coherent equality patterns must still be kept, as they are in the
source. This comparison does not replace them by a pre-Walsh tensor.

With the source's UNIFORM rate epsilon = n^(-1/2) times a fixed
logarithmic power, the two-root open tensor satisfies

```
sup_(j,c) ||J_(j,c;.)||F <= C epsilon^k,
||J||F <= C n epsilon^k,
(1/n)||J||F^2 <= C n epsilon^(2k) -> 0  (k>=2).
```

Only two gains are needed. Apply B D and B D_a B separately on the
two source-root axes before taking their common output diagonal.
These bounded operators and the final norm-one coordinate projection
preserve the displayed estimate. A rootwise absolute-sum estimate
would not preserve it. The mean diagonal D_a is deterministic.

An averaged small-cut statement alone cannot replace the uniform
epsilon estimate. Collision and approximation errors require the
separate averaged L2 passage against a bounded opposite kernel, with
all polynomial degrees fixed first. The source now says this
explicitly. Its final paragraph also correctly excludes treating the
literal residual BF-Z_P as an additional degree-M primitive.

This closes the stated fixed-stage mean-linear branch only. It does
not prove the entire rich returned-query comparison, a positive
high-degree source for every rich policy, or a global cap gain. The
remaining bounded-source, ordered-limit and joint-query assembly is
still explicitly OPEN in the rich-feedback checkpoint.
