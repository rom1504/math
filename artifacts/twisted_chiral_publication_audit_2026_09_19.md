# Closing verification and preservation audit

This records checks, not additional mathematical premises. Proof statements
and limitations are in `twisted_chiral_final_synthesis_2026_09_19.md`.

## Reconstructed and replayed on closing

- Sharp diamond integer identity:14 rational terms, all64 coefficient entries,
  weight4/3,1,900 lifted integer tests; independent sharpness reconstruction.
- Near-order audit:105 direct-spin cases; independent factor-one contraction,
  cycle-repair image containment and actual insertion coefficient expansion.
  The separate plain-scalar audit checks10,000,1,700 and800 cases respectively.
- Hadamard20 certificate: complete algebraic upper proof and a literal spin
  attaining40. It is not a global order20 lower certificate.
- Tensor/diagonal certificate:6,096 diagonal evaluations on84 small matrices;
  both actual20 parents independently reconstructed using integer sums.
- Exact local-stability identity:46 cases,2,256 sector permutations and120,672
  matching/permutation pairs. Complete small averages additionally agree with
  independent full-group scalar enumeration at child3 and4.
- Conditional support audit:24 Hall profiles,280 rational scaled-permanent
  tests and low-cap Hadamard examples at4,16,64. Independent proof review.
- Dominance support matcher:720 generic and720 actual-child sector tests
  against exhaustive bijections. An independent exchange-proof audit passes.

The large child9/10 censuses and complete twist-family searches were audited
from their source, exact coverage counts and independent programs, with all
stored parent witnesses independently checked. Their entire long runs were
not redundantly repeated during the closing minutes. Solver outcomes,
exhaustive arithmetic, finite examples and heuristic searches keep separate
labels. A cap38 search timeout/failure is not a lower bound.

## Dependency closure of the new canonical work

The closing Python certificate programs use explicit constants or tracked
result JSON files and tracked sibling imports. The wind tunnel compiles its
scratch executable automatically from tracked C++ source; scratch is an
output/build location, not an irreplaceable research dependency. Its complete
matrix inputs, seed, permutations, switches and matching vectors are retained.
The build provenance record independently reconstructs32 chiral executables
from15 sources and compares code/data sections. Existing software packages
are documented by the preservation runtime inventory.

No new canonical proof or replay has an undocumented ignored research input.
This is a source-level assessment, not a claim that a text scan can prove
dependency closure for arbitrary archived dynamic code. Historical experiments
can be restored using original paths recorded in the archive manifests.

## Preservation and intentionally untracked originals

The closing snapshot `research_archive/2026-09-19/205233Z` contains2,012
research payloads totaling170,486,266 bytes. All hashes verify. At the first
current-work check all2,012 originals matched, with no missing/changed research
or credential flags. Canonical additions after that snapshot are committed
directly; publication is followed by another current-work check.

The main publication commit8ccc43f was pushed normally. Its subsequent
current-work audit found1,976 remaining ignored/untracked research originals,
all matching their preserved payloads, with no missing/changed research or
credential flags. The lower count reflects canonical files now committed.
A final full wind-tunnel replay reproduced ALL151 histograms and summaries;
only its timestamp and elapsed time changed. The final scope note explains
that several zero-defect sample thresholds already lie below known family
minima and cannot falsify an asymptotic theorem with a subleading defect.

The final coverage audit checks that the parity-rounded oscillatory scalar
countermodel respects cap monotonicity, a summable doubling defect and the
proved near-order modulus. It still fails to converge. The director caught
and corrected a proposed additional signing-density monotonicity:5/15<9/21
at orders6 and7 refutes it. The analytic scalar example may obey that extra
hypothetical property, but it is not a theorem about M_n. This failed
inference is explicitly retained in the uniform artifact; no proved transfer
or convergence criterion depends on it.

Two old untracked originals intentionally remain:

1. `computations/decisive_independent_exact_range_scan_2026_09_07`: reviewed
   regenerable executable with preserved source/provenance.
2. `computations/results/flatify_director_rank_two_witness_profiles_2026_09_07_m32.json`:
  176,489,665-byte research output already preserved byte-for-byte in a
   tracked lossless compressed copy, indexed by
   `research_archive/reviewed_compressed_research.json`.

Ignored environments, caches and reviewed builds are excluded. Scratch
research originals remain in place, hash-preserved; none was discarded to
clean the worktree. The scanner retains14 historical/dynamic reference flags
for manual interpretation, not unresolved new theorem inputs.
