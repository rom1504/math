# Independent audit: critical landscapes above one half

2026-09-07. **PASS.** I independently read `principle_invent_2026_09_07_near_half_critical_landscape.md` in full and reconstructed its new block compiler and all-spin estimates. The fixed-degree expander existence is the same author-verified external input as in the paired construction; the new finite-field identities are proved elementarily in the artifact.

The community modification is valid: adding a 10-regular graph within each bipartition preserves the two-dimensional part-constant subspace, with eigenvalues110 and -90. On its orthogonal complement the operator norm is at most21+10. The subsequent matching switches have perturbation norm at most two, giving lambda_min(G)>=-92. The majority-rounding proof of the scale-dependent Cheeger constant keeps the smallest community scale and has the stated factor3a/4.

The masked tensor power of J4-2I4 is a legitimate symmetric regular Hadamard before masking. Deleting its diagonal and the110 graph-supported entries per row changes both its operator norm and its row-sum bound by at most111. The residual R1 is not assumed zero.

For the character block C of order q=9^k, the displayed difference-of-squares count proves C^2=qI-J and C1=0. Its diagonal augmentation C+I is FULL signs, including the cross-fibre matching coordinates. On normalized fibre constants the physical matrix acts as qG+R. On the orthogonal fibre modes it acts as R tensor(C+I)+I tensor C. These subspaces are exactly invariant, so no cross-mode energy term has been omitted.

For x_i=a_i1+y_i, the bulk energy is bounded by Lq u/2 with u=sum(1-a_i^2). Its norm ratio L/(qD) tends to1/(2c), giving a positive fixed margin precisely for c>1/2. The subsequence q/m tending to(2c/D)^2 follows from irrationality of log9/log4 with both exponents growing.

The soft-spin randomization is exact: for independent signs of means a_i,

    Dm-a^TGa=a^T L_G a+D u=4 E cut_G(Y).

Writing T for the number of negative signs, VarT<=ET=mr and r<=1/2 imply E min(T,m-T)>=mr/4 for m>=4. The increasing convex function psi gives E psi(min(T,m-T)/m)>=psi(r)/16. This does not assume an unproved rounding monotonicity.

The residual identity with z=(1-a)/2 pays

    1^TR1-a^TRa>=-4(||R||+||R1||_infinity)mr.

Combining this with the bulk margin gives the claimed positive deficit. Since q h_m/K tends to infinity, half the expected-cut contribution pays the entire residual error uniformly in r; the other half supplies the critical barrier. The physical Hamming fraction really is sum(1-a_i)/(2m), including nonconstant fibre words.

For the negative polarity, the graph lower eigenvalue and both residual R terms give

    Q0+H >=(q^2/2)[18||a||^2+(D-L/q)u]-(qK/2)m.

This is bounded below by a fixed positive multiple of q^2m. Thus no negative-polarity word can be an absolute ground, and the positive coercivity leaves only the two constant words.

The community-constant excitations have no bulk component. Their exact deficit is2q^2 cut_G+2q cut_R. The residual cut is at most Kmr_j in absolute value, and its ratio to the asserted critical graph contribution vanishes uniformly through r_J. This proves the matching upper scale without suppressing an R correction.

The result is an actual full-sign construction at every prescribed coefficient c>1/2. It escapes the sparse two-spin compiler's forced amplified-complement floor by using growing fibre dimension. It does not reach the known optimal interval below one half, and the positive bulk margin degenerates as c approaches that threshold.
