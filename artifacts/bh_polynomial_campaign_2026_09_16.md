# Polynomial Boolean Bohnenblust--Hille investigation

Status: ACTIVE; proofs below are not implied by this operational record.
Authorized by the user for two hours, starting2026-09-16 at05:13:45UTC,
ending about07:13:45UTC. Target paper: arXiv2609.12427v1,
Paata Ivanisvili, *Polynomial growth of Bohnenblust--Hille constants
on the Hamming cube*. Do not restart the general research loop afterward.

Write access verified by creating this file with apply_patch. Existing
untracked binary and oversized raw output are preserved working originals;
do not delete or overwrite them. Use only /home/math/quadra/tmp for
temporary research and preserve substantive work at publication.

The investigation will reconstruct the theorem/proof first, then compare
power/convolution and proof-mechanism applications with existing work.
Independent adversarial verification is required for promising deductions.
Evidence categories: proved here, imported theorem with checked hypotheses,
exact computation, heuristic, open. No original bound improvement is
asserted by the campaign authorization.

## First substantive checkpoint, around06:00UTC

Original interval unchanged; convergence remains open. The primary theorem
and complete proof have been independently reconstructed. New proved work:

- Exact boundary Renyi-entropy identity for reduced powers, including
  a simultaneous/adaptive sublinear-power obstruction. Linear powers
  recover the old signed all-boundary moments; superlinear powers recover
  the original cap. The older subexponential constant already suffices
  for the same root limits.
- Uniform all-signing scalar-filter response at degree o(sqrt(n));
  bounded-operator rare tails extend the obstruction for the current
  polynomial BH exponents to EVERY growing sublinear filter degree.
- A parameter refinement of the paper proves weighted O(m^3) and full
  O(m^(11/2)) growth. This is a proved secondary analytic consequence,
  not a claim of external novelty or original signing progress.
- Exact joint-field and weighted-port inequalities have been audited.
  The former's tested bootstraps cannot improve the current lower bound;
  the latter improves a relative microscopic-variance estimate, not a
  leading construction sector.

Canonical proof/audit files are the `artifacts/bh_2026_09_16_*.md` notes;
replay programs/results use the same date prefix in `computations/`.
The director's nonlinear-filter proof was checked by both independent
researchers. Actual stored cap witnesses6--14 and24 nonlinear filters
were replayed; their optimality is not an input to the new theorems.
Only integer/rational checks are certificates; numerical norms and
optimization diagnostics are labeled separately.

Next discriminating work, within the SAME bounded session: verify the
Boolean Bohr-radius connection and its degree localization; finish the
scope audit of generic joint-square-function and column-profile escapes.
This checkpoint is not the end of the two-hour investigation.
