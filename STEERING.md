# Strategic steering

Evidence cutoff: renewed campaign checkpoint 1, ledger Section 10.150.1.
Status: **six-hour recursive-construction campaign active**,
Started 2026-09-06 15:15:02 UTC from b1745cc; approximate end 22:15 UTC,
excluding the service interruption around 16:05–17:02.
Convergence and nonconvergence remain open; convergence to 1/2 is excluded.

## User-stated objective and workflow directives

Determine whether M_n/n^(3/2) converges, without presuming its value.
Convergence to any constant or rigorous nonconvergence is success.

The user authorizes six additional active hours. Initially use three
complementary researchers: independent reconstruction, near-optimal seed
transfer, and adversarial/alternative work. The director actively derives,
integrates, verifies and owns route selection. Preserve the interval and
proof artifacts, correcting claims if verification requires it.
Keep useful feedback tools secondary; decimal gains are not convergence.
Reconstruct dependencies rather than treating audit verdicts as proof steps.
Commit substantive checkpoints roughly hourly and continue after them.

Workspace writes, shell/Python execution, and normal Git push are available.
Never use /tmp; follow the README's verification and publication discipline.
No execution blocker is present. Mathematical judgments below are agent-owned.

## Verified frontier and dependencies

```math
0.4333221116640807
\le\liminf_n M_n/n^{3/2}
\le\limsup_n M_n/n^{3/2}\le0.499432220485404<1/2.
```

The exact upper endpoint is 1/2-a/(8sqrt(31/32)), where
a=91470529542342299/20460000000000000000. Its new standalone reconstruction
and exact rational replay passed. The proof now uses direct stopping at the
conditional-variance envelope T, avoiding the smaller-envelope fixed-point
detour, and terminal Hadamards generated by H2 and H12, avoiding prime gaps.
Fixed margin -> fixed recursive depth -> all-order limit -> margin limit.
[Standalone proof](artifacts/transfer_reconstruction_standalone_2026_09_06.md).

The lower endpoint retains its actual marked/two-Gaussian response proof and
exact 256-cell interval certificate. Finite approximation precedes order;
spectral deletion cutoff is removed last. No new lower claim is made here.
Recorded M_3,...,M_14=(3,4,4,5,9,10,12,13,17,18,20,21).
The n=11,13 lower solver certificates were not rerun in this campaign.

## Current assessment

The upper construction is sound but is not yet a seed-transfer theorem.
Its outer signs can be absorbed into arbitrary Hadamard column gauges, and
full weaving has actual normalized cap at least 1/2 for every seed.
Balanced fibre retention p has the exact Boolean floor sqrt(p)/2.
The strict upper theorem therefore makes full weaving and self-squaring
genuinely lossy even on actual minimizing seeds.

The conditional-variance factorization extends to every orthogonal gate
and unequal parent marginals, with total correlation as the exact charge.
Every fixed real Hadamard gate has the same Gaussian zero-drift set.
This removes binary-gate restrictions, not the original liminf comparison.

A stronger NEW limitation holds before any Bellman approximation:
the exact full-spin row-permanent certificate cannot certify below
sqrt(15)/8. Positive Sinkhorn scaling plus van der Waerden gives the finite
orbital lower bound L_t(v)>=exp[m g_t(||v||²/m)]. Jensen extends it to
arbitrary nonorthogonal sign bases with their variable norm retained.
No recursion, parameter choice, or improved scalar row potential can evade
this same certificate's floor. This is NOT a lower bound for actual signings.
[Proof](artifacts/transfer_director_exact_permanent_floor_2026_09_06.md).

Near-optimality alone also cannot control a full-rank determinant prefactor:
o(n) twin pairs can be planted with o(n^(3/2)) cap loss, while every sign
diagonal/precision/elimination order has a divergent normalized prefactor.
These defects are removable in this example. A well-chosen near-optimal
sequence is not excluded. [Proof](artifacts/transfer_seed_twins_precision_obstruction_2026_09_06.md).

## Exact sufficient convergence target

Let ell=liminf M_n/n^(3/2). It suffices to find seeds A_j of orders k_j->infinity
with Q(A_j)/k_j^(3/2)->ell and, for each fixed j, constructions on a relatively
dense sequence of target orders with limsup normalized cap at most
Q(A_j)/k_j^(3/2)+epsilon_j, epsilon_j->0.
First take target order to infinity, using principal restriction to fill all
orders; only then send j to infinity. Uniform recursive depth in j is not
required. No presently proved construction provides this comparison.

A new seed observable is useful only with an actual quantitative cap bound.
The softened Gaussian-kernel route retains a quadratic Gram optimization;
naming or regularizing it does not control the Boolean seed loss.

## Ranked active work and falsifiers

1. Test seed-sensitive restricted tensor constructions and the stabilized
   Hadamard norm R versus its absolute-PSD majorant T. R<=T is proved;
   R=T is OPEN, not a consequence of ordinary quantum-bias or all-win
   parallel-repetition theorems. A genuine near-minimizer gap would rule out
   that lift, not convergence.
2. Determine whether the new permanent floor is sharp for the ACTUAL joint
   annealed spin/row law. Homogeneous-profile graph counting and typical
   retained-Hadamard spectra are the next concrete tests. If sharp, further
   scalar/Finner refinement is not the route; correlated spin events need
   a genuinely different estimate.
3. Keep actual marked feedback as a secondary tool. Its retained-energy
   identities remain useful, but the terminal functional below .45 is not
   worth retuning. The high-value arbitrary-minimizer innovation is still
   missing, and involution identities cannot replace it.
4. Genuine nonconvergence requires two infinite separated order subsequences,
   not a certificate obstruction or an algebraic-family floor.

Uniform random restrictions of actual bounded-cap seeds at order
k=floor(sqrt(log_2(n)/2)) have cap at least (2/3)sqrt(2/pi)>1/2 with high
probability. Useful restrictions are exponentially rare at that scale.
This does not exclude exceptional restrictions or near-order transfer.

## Continuing discipline

Checkpoint 1 removes proof dependencies and establishes scoped seed/
certificate obstructions; it does not close the liminf-to-all-order gap.
Continue the six active hours (approximately through 22:15 UTC). Preserve old local-profile,
Cayley, action and Gibbs no-gos only with their proved hypotheses.
Next substantial director review in about one hour. If numbered waves resume,
Wave 61 remains the scheduled blank-slate boundary. Full history is in Git
and ledger; this assessment is not another detailed log.
