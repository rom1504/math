# Benchmark scorecard

Compact scorecard for the campaign begun at `266d101`.  “Independent” means
the operational state was frozen before comparison with the named classical
representation.  Detailed proofs and exact checks are in `drafts/` and
`experiments/`; this file does not promote an unaudited draft by itself.

| Benchmark | State predicted from future responses | Classical or comparison state | Independent? | Exact / approximate complexity | Verdict |
|---|---|---|---:|---|---|
| Viterbi / finite-state best path | endpoint survivor vector modulo one additive scalar | Viterbi survivor metric; max-plus transfer vector | yes | exact `q-1` real coordinates; `Theta((D/epsilon)^(q-1))` projective codebook; global zero-temperature contraction is `0` only at max-plus rank one and otherwise `1` | pass; predicts a sharp mixing obstruction and near-rank-one reset |
| Binary linear-code trellis | reachable past modulo the past-supported subcode; equivalently the partial syndrome in `im H_P intersect im H_F` | minimal trellis quotient `C/(C_P direct-sum C_F)` | yes | `2^r` labels, `r=dim C-dim C_P-dim C_F`; rich weighted tables need `Theta(2^r log(B/epsilon))` absolute bits | pass; strict quotient and raw-interface no-go both predicted |
| Potts tree / finite-state graphical model | baseline plus clipped projective message `C_K={r in [-K,0]^q:max r=0}` | max-sum / junction-tree message | yes | exact projective dimension `q-1`; sharp codebook `Theta_q((K/epsilon)^(q-1))`; an exact finite lattice closes after one-time factor rounding | pass; a new scoped rate and reusable approximation accompany the classical state |
| Discounted deterministic control | full terminal value vector, projectively modulo one scalar | Bellman value function / discounted semigroup | yes | exact depth-`h` metric is `lambda^h` times sup or half-oscillation; bounded-reward rate is `Theta(n log(1+B_H lambda^h/epsilon))`; fresh errors cost `1/(1-lambda)` | pass; contraction produces a sharp forgetting-time information law |
| Finite-width Ising | projective boundary response or smaller rational anticipatory carrier under restricted futures | max-plus transfer matrix | earlier solution-hidden run | exact size depends on declared futures; unrestricted futures expose the whole table | pass; calibrated the query dependence |
| Bounded-separator Max-Cut/CSP | projective boundary response table | treewidth dynamic-programming table | earlier solution-hidden run | pure futures expose the full one-Lipschitz response ball; logarithmic cover number is exponential in width | pass; local sensitivity is not approximate compression |
| Fixed-rank Boolean bridge | concave upper roof over the aggregate feature | finite-rank mean-field / support-function state | derived directly | exact associative roof algebra; fixed-error cells `exp(O(r log(1/epsilon)))`; worst cases need `2^(Omega(r))` bits | pass; exact and approximate complexity sharply separate |
| Scale-sensitive spectral bridge | roof over singular features above the target-scale threshold | truncated SVD plus low-rank factor model | derived and independently audited | tail error `n sigma_(r+1)` is uniform over all futures; bounded feature-visible ports need `exp(O(r_epsilon log(1/epsilon)))` local cells; separator size still controls global memory | pass as a multi-benchmark local law, not a global dense-graph quotient |
| Bounded-operator dense sign bridge | linear set of scale-visible singular features | stable-rank/Frobenius mass | derived and independently audited | `r_epsilon >= n(1-epsilon^2)/(C^2-epsilon^2)` under `||R||<=C sqrt(n)`; Hadamard is sharp | decisive ceiling for SVD compression, but not for nonlinear symmetry quotients |
| Degree-one matching bridge | bridge response transform on the live interface | separator DP with extensive interface | no classical state supplied | arbitrary children require `exp(Omega(n))` response bits at error `Theta(n)` | negative; bounded degree is not a compression parameter |
| Vertex-cover bridge width `k` | `2^k`-entry conditioned bridge-response table, quotiented further only by numerical envelope collisions | separator / bucket-elimination table | yes | worst-case projective rate `Theta(2^k log(1+D/epsilon))`; matching attains it | pass pending final audit; explains why degree-one matching is hard when its live cover is extensive |
| Structured full-rank dense bridge | one magnetization per common permutation orbit, plus signed cycle gauge | rearrangement / mean-field count state | derived directly | polynomial joint search for fixed orbit count; a restricted thermodynamic limit follows; not an arbitrary frozen-fragment quotient | pass; rank is not the controlling resource |
| Random dense sign bridge | no small all-landscape state: exponentially many exposed response coordinates | no proposed classical compression | theorem independently audited | arbitrary children require `exp(Omega(n))` bits at error `Theta(n^(3/2))`; linear children already require `Omega(n)` bits | strong negative endpoint; special quadratic child classes remain open |
| Complete sign-quadratic children across a dense sign bridge | coefficient signing, with response collisions allowed | quadratic coefficient description | lower and upper developed by opposing agents; lower independently audited | projective packing forces `Omega(n)` bits; a sparse weighted surrogate stores at most `(1-epsilon^2/4)binom(n,2)+O(log n)` bits, still `Theta(n^2)` | pass as an information bracket; this planted lower has `Theta(n^2)` cap and does not explain the bounded-cap rate |
| Exact cap-`1/2` Walsh sign quadratics | coordinate switch for the full orbit; smaller truth table/permutation for explicit subfamilies | regular Hadamard/Walsh switching representation | yes; all three packing theorems independently audited | near-top entropy amplification gives matching `Theta(n)` bits for the full orbit; explicit Boolean-table and permutation families give `Theta(sqrt(n))` and `Theta(sqrt(n)log n)` states with deterministic Walsh bridge | strong near-original pass; bounded cap alone does not collapse response memory |
| Repeated Walsh-bridge graph composition | unrooted `(Gram,relations)` state; rooted relation form; local `(kappa,J[,Z])` amalgamation; Kronecker carrier for arbitrary truth tables | no classical extremal quotient supplied | yes; relative-involution, orbit, gluing, Weyl/ambient-symmetry, query-local, and flux-packing arguments independently audited | exact `O(k^2)` global orbit quotient; the unrooted landscape forgets `Z`; deleting interaction mass `d` gives error `d n^(3/2)` and unit paths need `O(t/eta)` bits at error `eta t n^(3/2)`; independent paths force one `J` bit each; `h` relation triangles inside one common connected support expose `h` independent Gram-flux bits at a fixed one-block gap; unrestricted gluing retains `rs` cross-form and `r^2+O(1)` intersection orbit memory | strong pass: first strict Boolean-extremal quotient with exact nonlocal composition, rooted/unrooted symmetry transition, weighted approximate locality, and scalable scalar-visible holonomy; an extensive total-system flux rate remains open |
| State-local Walsh flux at total scale | bounded onsite hidden state plus arbitrary public scalar bridge landscape | Lipschitz/influence bound | derived and independently adversarially audited | response diameter at most `DN^(3/2)/sqrt(k)`; unequal disjoint-cell `epsilon N^(3/2)` packing has at most `2^(O(L^2/epsilon^2))` states; one local bit needs `Omega(k^(3/2))` changed unit atoms for a constant neighboring gap | decisive no-go for the current flux architecture: public connectivity cannot amplify local state; only genuinely state-dependent cross-block broadcast can escape |
| Alternating-form state-dependent broadcast | `Theta(k)`-bit presymplectic form evaluated on one public label list | dense bicharacter code plus shared random flat dressing | derived and independently audited | `2^(Theta(k))` bounded-cap exact sign children have fixed `Theta(k^(3/2))` response separation under a predeclared same-support overlay language | strong positive escape and sharp scope boundary: dense hidden incidence works, but uses quadratic public advice and does not preserve exact-sign closure under context |
| Bounded-fan-in quadratic broadcast | hidden coordinates charged to the bounded atoms whose coefficients read them | incidence/influence accounting plus a cellwise switching code | derived and independently audited | at total scale `h=O(t sqrt N)` and this is sharp, including an `exp(Omega(t sqrt N))` response packing; the Gram code has average fan-in `Theta(N)` in every basis | complete unrestricted fan-in law; flatness, public-description length, and disjoint exact-sign closure remain separate resources |
| Short-seed flat Gram broadcast | small-bias label sampler plus a `6k`-wise-independent polynomial-trace edge seed | bounded-independence spectral derandomization | derived and independently audited | `O(k log k)` shared bits support `2^(Theta(k))` flat children with fixed `k^(3/2)` response separation; this seed length is optimal among exact `6k`-wise-independent edge spaces | strong near-original improvement: quadratic public advice is eliminated; exponential same-support queries and exact-sign/disjoint closure remain |
| Exact disjoint star compilation | edge variables, or independent absolute linear selectors | quadratization / star gadgets | derived and independently audited | exact sparse identity uses `binom(k,2)` auxiliaries; bounded fan-in obeys `sum d_a^(3/2)>=k(k-1)`; full-support stars need `Omega(k^2)` auxiliaries for both orientations | decisive scale boundary: exact semantics alone do not preserve `N^(3/2)` normalization; correlated auxiliary interaction is necessary for any linear-order escape |
| Interacting exact-sign selector compilation | correlated affine envelope indexed by auxiliary spins | quadratized pseudo-Boolean optimization | derived and independently audited | universal cut-shell approximation needs `m>=(8log2)^(-1/3)k-o(k)` for complete old--new incidence; bounded-cap `N=O(k)` parents have only `O(k^(3/2))` response oscillation and cannot approximate its `Theta(k^2)` shell | strong architecture-level no-go, but flat Gram-specific linear interacting compilers remain open |
| Exact disjoint coordinate pin | rank-one sign bridge plus ferromagnetic auxiliary clique | query-dependent penalty gadget | derived and independently audited | on `N=2k`, response is exactly `(3k^2-k)/2+H_A(u)`; the flat Gram packing retains gap `N^(3/2)/32` and `Omega(N)` bits | decisive positive metric closure: disjoint complete signs and linear order are possible, but the common `Theta(N^2)` calibration prevents low-cap use |
| Universal coordinate locking | effective future landscape required to pin one old state for every sign child | robust penalty / exposed-face margin | derived and independently audited | any linear-order exact pin has parent cap at least `k^2/4-O(1)`; a fixed all-duplicate pair lock collapses to one balance equation and has nonduplicate ties | decisive low-cap no-go for universal pinned-witness compilers; child-dependent optimizer switching remains open |
| Flat signed-contrast exposure | signed quadratic sign sets and antipodal selector covering | degree-two hypercontractivity | derived and independently audited | every signed contrast has exposed mass at least `1/324`; flat Gram compilers need `Omega(k^(2/3))` general or `Omega(k)` flat-cross auxiliaries when the signed orientation is exposed | strong lower law with sharp scope; a block-clique family proves absolute-channel full entropy false |
| Quadratic-character equality locks | common-gauge pullback plus intended-copy bridge | Boolean character algebra / dominant penalty | derived and independently audited | full pair-character preservation is only signed permutation plus gauge; every fixed bridge has worst lock defect at least `k sqrt(k/2)-sqrt(3)k`; bounded replication retains `Omega(k^(3/2))` | decisive no-go for full-algebra dominant locking, while query-dependent metric pins and restricted alternating pullbacks remain outside scope |
| Bounded-cap optimizer fibres | approximate reusable old-witness dictionary | optimizer support / evaluation sketch | derived and independently audited | a common fibre contains `2^(binom(k,2)-k)` bounded-cap children; an `exp(alpha k)` response packing nevertheless needs `Omega(k)` approximate witnesses | principled revision: cap does not force switching, but response separation forces extensive common-query or child-dependent witness support |
| Translation-orbit extremal queries | transversal of the threshold witness set | covering codes / Cramer large deviations | derived and independently audited | minimum coordinate library is `1/p` up to `log|G|`; product rate is exactly `I(a)`; flat exact-sign switched pairs can need `exp(Omega(k))` pins | strong multi-model law joining contextual and rare-event branches; arbitrary nonlinear contexts and interacting compositions remain outside scope |
| Regular-Hadamard amplification | normalized fixed-port cross-correlation set | tensor/direct-limit response carrier | derived and independently audited | fixed dimension `d(d+1)/2`; external `epsilon`-net at most `(1+2/epsilon)^(d(d+1)/2)` independent of depth; exact witness lift gives Hausdorff convergence | strong restricted near-original pass: dense hollow sign hierarchies have normalized limits on geometric orders; arbitrary orders and perturbations remain open |
| Summably perturbed amplification | normalized fixed-port response kernel under one common Boolean lift | approximate directed system / recovery sequence | derived and independently audited | exact signed-replication distortion in `infinity->1`; summable directed error forces a limit; matching flips give `Theta(N)` non-tensor edits with defect `2/sqrt(N)` | strong near-original pass: perturbative recovery is genuine; square-summable-drift falsifier and unsynchronized order phases delimit it |
| Automatic regular-Hadamard prefix hierarchy | continuous base-`h` mantissa profile | discrete-scale/automatic tensor phase | yes; exact Walsh certificate and general phase theorem independently audited | every fixed outer phase converges; the full all-order response converges uniformly to a continuous phase profile; the order-four Walsh profile has `L(1)=L(4)=1/2` but `L(3)>=89/(96sqrt(3))` | strong near-original falsifier: all-order realization and phasewise recovery do not imply synchronization; one explicit dense signing sequence is nonconvergent, though this says nothing directly about the minima `M_n` |
| Automatic-phase averaging | logarithmic pushforward law versus ordered mantissa profile | logarithmic/regular-variation sampling | derived and independently audited | logarithmic empirical law is unique; every positive power bias retains an injective phase response `C_alpha`; equal log laws can have different ordered phases | pass as an information-boundary benchmark: thermodynamic averaging may be a strict noncomposable quotient |
| Phase-refresh hierarchy | averaged Boolean pullback plus phase Markov kernel | Doeblin mixing / maximum principle | derived and independently audited | a full-support refresh mass `alpha_r` collapses the limiting phase when recovery plus operator defect is `o(alpha_r)`; a two-state example is scale-sharp; the Walsh obstruction prices every such certificate by at least `0.0605036` | strong positive cross-scale theorem; query obligation is strict, while certificate description complexity remains model-dependent |
| Expander phase refresh | common-stationary semantic phase plus transfer toll | `L^2` Markov mixing / response recovery | derived and independently audited | state law `log S >= log(1/rho)D/delta-O(log(B/D)+log(1/kappa))`; Walsh at `delta=O(N^(-1/2))`, `rho=1/2` needs `Omega(sqrt N)` bits; pure gauge orbits refresh with one bit despite operator diameter two | strong information law: local expander presentation cannot make semantic phase synchronization cheap |
| Finite signed feature dictionary | one public sparse mask plus retained signs | importance sampling / uniform sparsification | generalized only after quadratic theorem | saves `Omega(min{m,E^2m/(V_Phi log|X|)})` bits and protects all shared max-type futures | new multi-model upper law; not an invariant internal quotient |
| Generic random weighted dense Max-Cut at scale `n^2` | density/block representative in cut norm | weak regularity graphon state | literature-grounded | exact table exponential, but one block gives all-future `O(n^(3/2))=o(n^2)` error | pass at dense scale; fails to resolve `n^(3/2)` scale |
| Mean-field BEG microcanonical landscape | usc log-count hypograph over occupation/energy descriptors | large-deviation microcanonical entropy | literature-grounded and independently audited | exact sup-convolution recovery under subexponential decomposition count; bounded-temperature pressure loses a rare maximum; exponential descriptor images and subexponential decorations are sharp falsifiers | scoped pass for leading-rate rare-event compactness |
| Finite-type multiscale count skeleton | lexicographic valuation profile at finitely many separated speeds | higher-order large deviations / tropical count algebra | literature-grounded and independently audited | exact composition and abstract integer recovery for fixed descriptors; uniform growth allowed only below the smallest retained speed; Vandermonde saddle mass falsifies bare pointwise profiles | scoped pass, kept warm: genuine subleading resolution but not an orthogonal state until tangent mass closes |
| Gaussian tangent convolution | `(height,mean,covariance,log-power,total-mass amplitude)` | discrete Laplace / Gaussian convolution parameters | derived after a literature-grounded falsifier; independently audited twice | fixed dimension `d(d+1)/2+d+3`; exact associative asymptotic composition, uniform compact-parameter law, and finite integer all-order recovery; quartic saddles falsify universality | strong scoped pass: first strict finite response algebra repairing the second-speed saddle-mass defect |
| Power-exponential tangent convolution | leading power coefficient plus the actual normalized tangent density | homogeneous infimal convolution / generalized Gaussian | derived and independently audited | leading roofs close for every `p>1`; central and off-centre log powers differ; the fixed power-exponential tangent family is convolution-closed only for `p=2` | sharp negative: finite Gaussian closure is rigid, while non-Gaussian tangent state grows beyond the leading roof |
| Boundary-case branching-random-walk extremes | derivative mass `Z` for unmarked limiting Laplace queries | derivative martingale / decorated Cox process | primary-literature scout, theorem hypotheses checked | one scalar after critical renormalization; finite depth requires `(W,Z)` and marked futures require a mass measure | orthogonal pass; a macroscopic state can emerge only in the limit |
| Finite-width chain with discrete adversarial disorder | lower spectral growth rate; under positivity, normalized projective cavity plus additive potential | transfer matrix / lower spectral radius | literature-grounded and independently audited | pressure and ground-state density converge; a `delta`-net has `O(delta^{-(q-1)})` states and mean-pressure error `delta/(1-kappa)` | orthogonal pass; nonconvex disorder retained, but dense cuts have leading `n^(3/2)` response and exponential transfer width |
| Deterministic de Bruijn carrier | semantic response is scalar; anticipatory certificate retains suffix phase | finite automaton / subset carrier | derived directly | one semantic and one forward state versus `q^m` exact anticipatory states; bounded rooted probes recover `q^m` states only under a gap/filter condition | sharp falsifier and repaired observability theorem |
| Bounded-delay weighted residuals | normalized suffix row plus scalar toll; optional greatest threshold-support core | weighted determinization / max-plus residual | derived, then independently compared with Mohri twins theory | at most `|E|^D` recurrent profiles; context cycles give exact response and potential drift; complete terminal pins give metric `osc(p-p')/2`; one rank-one two-letter system has an empty support core but identically zero scalar response | strong pass with principled revision: residual response, witness certificate, and query observability are distinct resources |
| Approximate residual shells | last-suffix projective code for terminal queries, plus a separate directed compatibility cocycle for accumulated reward | approximate weighted automata / width-one switching Ising | derived and independently audited | terminal error `2(epsilon+eta)` is depth-independent; two exact resets force sharp `delta/4` one-state reward rate; any bounded weighted automaton embeds in a one-profile shell of radius `alpha/2` | strong dynamic pass: static forgetting and cumulative reward memory are quantitatively incomparable |
| Contracting Markov/control fibres | terminal and fresh-reward dictionaries on the centred fibre, plus the scalar recurrent cohomology class | Poisson/Markov reward decomposition plus graph cycle space | derived and independently audited twice | code size `N_U(epsilon)N_B((1-rho)epsilon)(1+2L/epsilon)^(|E|-|Q|+1)`; error `epsilon t+O_G(epsilon)`; all three scales are sharp | strong unifying pass: first quantitative law adding static response entropy at the forgetting scale to persistent dynamic cycle memory; assumes rather than discovers the common-law carrier |
| Hidden broadcast on a contracting carrier | terminal/centred incidence paid at the forgetting scale; scalar hidden incidence charged to recurrent cohomology | bounded-influence accounting plus Markov/cycle decomposition | derived and independently audited | sum of persistent neighbouring response rates is at most scalar bit--atom incidence; finite-depth law retains `rho^L` and `(1-rho^L)/(1-rho)` explicitly | multi-model unification: predicts when static broadcast survives repeated composition and rules out contraction as payment for hidden scalar compatibility |
| Orthogonal-sign planted synchronization | normalized Hadamard transform plus quadratic relation deficit | spectral pullback / regularizer code | derived and independently audited | exact deficit `sqrt(k) dist^2/2`; a bounded-cap exact-sign parent defeats one planted relation by at least `k^(3/2)/8` | sharp negative: a perfect Boolean pullback and correct cap scale do not create a reusable witness without local alignment |
| Bounded-cap Walsh anti-pin compilation | switch label through a `sqrt(n)` repeated-column query port | no classical compiler supplied | derived and independently audited | `exp(Omega(n))` cap-`n^(3/2)/2` children; exact parents of order `n+sqrt(n)` and cap `O(n^(3/2))`; projective metric distortion at most eight; `sqrt(n)` port width is order-optimal in this architecture | strong near-original positive: first exact-disjoint bounded-cap contextual metric compiler; reusable multi-port congruence remains open |
| Fixed-port spectral anti-pins | two resolvent features per pair; at `ell` ports, the two Gram matrices `(G,R)` | trust-region / spectral feature relaxation | derived and independently audited | two-eigenvalue case collapses to one Rayleigh coordinate; fixed-port spherical certificate has `O(ell^2 log n)` bits and `O_ell(n)` exact-sign completion error | positive one-layer algebra; a leading orthogonal-eigenvector gap proves marginal port states do not amalgamate without cross Gram data |
| Cross-Gram macroscopic dimension | the realizable pair of PSD spectral sectors `(G+R)/2,(G-R)/2` | elliptope / Gram geometry | derived and independently audited | every coordinatewise independent amplitude-`delta` affine cube has at most `2p/delta^2` bits; tensor Walsh attains `Theta(p)`; total-scale fixed-arity law includes `(n/N)^3` | sharp revision: the exact `p^2` table is not a quadratic information lower bound; the next benchmark closes the collective fixed-scale entropy question |
| Collective Gram response geometry | trace-bounded PSD spectral sectors in the signed quadratic query pseudometric | spectral truncation / factor nets | derived and independently audited | fixed-scale covering and Boolean-realizable packing entropies are both `Theta(p)`; at `mp/r=O(1)` response error is `O(sqrt(eta))`, sharply, and an `epsilon` carrier costs `O(p epsilon^(-2) log(1/epsilon))` log states | strong positive compression law for the spherical carrier; exact Boolean recovery and reusable physical gluing remain open |
| PSD Gram compatibility fibre | marginal low-rank factor frames plus a contraction; globally, one block correlation operator | Douglas factorization / PSD completion | derived and independently audited | exact fibre has `r_1r_2` coordinates; at fixed collective error its conditional cover is `O_epsilon(1)` in port count, with Boolean lower entropy `Omega(epsilon^(-1)log(1/epsilon))`; marginal truncation pays a sharp square-root cross error | strong reusable spherical gluing law; shows conditional composition cost can be smaller than marginal response entropy, but Boolean integrality remains |
| Boolean recovery of the spherical Gram carrier | exact cube response versus trust-region decoder | `l_1/l_2` support geometry | derived and independently audited | two orthogonal Boolean top poles at `mp/r=1` have exact normalized gap `(sqrt(2)-1)/2`, scalable under regular-Walsh tensors and stable under exact-sign completion | decisive falsifier for uniform spherical rounding; does not yet rule out a different Boolean decoder from the same Gram coordinates |
| Exposed Boolean synchronization | one near-global spherical optimizer plus its coordinate-flatness deficit | coordinatewise sign rounding / support geometry | derived and independently audited | gap at most `xi+(1+c)sqrt(2phi)`; no whole `d>=2` subspace is uniformly flat; a distinct close-pole Walsh family at `c=1` has exact gap `O(n^(-1/2))` | positive near-original recovery theorem for a strict rank-two family; global channel selection and reuse remain open |
| Common-pole trust recovery | one Boolean top pole plus average absolute port correlation | planted witness / tensor support | derived and independently audited | normalized gap at most `c delta`; `1-delta` multiplies exactly under tensor products; equal-delta order-four examples have different responses | pass as a strict composable recovery certificate, explicitly not a full response quotient |
| Active-eigenspace Boolean recovery | near-eigen support body or angular witness net | spherical covering / convex support | derived and independently audited | uniform error `eta` over `d` active directions needs at least `(beta/(2eta))^((d-1)/2)` explicit witnesses and matching-order extraction; exact eigen witnesses have an error floor | new response rate-distortion law; finite port languages and algebraic generation require separate accounting |
| Hierarchical exposed flatness | additive `(L2 mass,L1 mass)` of one supplied optimizer | sign rounding / block norm decomposition | derived and independently audited | exact chain rule; depth error depends on the sum of allocation defects; a binary amplitude tensor makes the normalized gap tend to one despite Boolean leaves | strong dynamic falsifier and recovery law; it does not select among switching optimizers |
| Generic eigenvector flatness | normalized `l_1` deficit of an exposed spherical pole | Wigner eigenvector universality / explicit block signing | literature-grounded plus exact falsifier | sign-Wigner eigenvectors tend to `1-sqrt(2/pi)` deficit; a hollow two-block signing tends to `1-cos(pi/8)` | rejects generic delocalization as a recovery mechanism; near-minimizer-specific rigidity remains open |
| Orientation under quadratic continuations | one relative channel-orientation bit, indexed by future cap budget | signed two-channel / block spectral state | derived and independently audited | universal gap ceiling `2Q(C)`; unrestricted sign futures have sharp width threshold `n^(3/4)` via a biased flat bridge and clique, while internally `O(m^(3/2))` futures need linear width | sharp dynamic law: contextual equivalence must declare both continuation width and internal extremal budget |
| Regular-Hadamard orientation networks | projective signed-graph switching class | gauge-fixed signed graph / common Kronecker factor | derived and independently audited | connected carrier has one bit per edge; joining `s` pieces through `r` edges has exact `2^r` fibre split into `s-1` relative antipodes and `r-s+1` cycle fluxes; both have leading witnesses | strong near-original dynamic pass: exact strict quotient and associative gluing law; restricted to one common Hadamard factor |
| Boolean linear port systems | projective row histogram; at fixed error, an empirical zonoid coreset | zonotope support / finite-group Fourier convolution | derived and independently audited | exact quotient has `binom(n+2^(p-1)-1,2^(p-1)-1)` states; fixed-error covering entropy is sharply `Theta_eta(p)` bits | strong pass: predicts both exponential exact feature dimension and linear response entropy; Gram is complete only through three ports |
| Mergeable Boolean port unions | exact count plus shared min-priority row reservoirs | min-hash / mergeable sampling | derived and independently audited | `k=O(eta^(-2)(1+log(T/delta)))` samples certify all `T` declared nodes with depth-independent error | dynamic pass for declared disjoint occurrence trees; adaptive or reconvergent overlap is correctly excluded |
| Tensor Boolean port systems | projective histogram convolution; iid occurrence products; uniform-component forgetting | finite-group convolution / Doeblin mixing | derived and independently audited | semantic error is 1-Lipschitz per factor and can accumulate with sharp factor `L`; independent occurrence trees have depth-independent sampling error; uniform mass `alpha` contracts by `1-alpha` | strong unifying pass: static response, reuse memory, and forgetting separate exactly in one algebra |
| Equal Gram--Rayleigh Boolean trust systems | higher projective row-pattern channel beyond `(G,R)` | spherical trust-region Gram carrier | two independent seed/tensor derivations and exact audits | common dense regular-Hadamard states at `p=4,mp/r=4` differ by at least `rn/8`; common exact-sign completion loses only `O(n)` | A-level information falsifier: every `(G,R)` decoder has fixed leading error, not merely the spherical decoder |
| Odd product-closed dense bridges | affine multiplicative top-pole coset plus projective histogram | Boolean Fourier selector algebra | derived from contextual response and independently audited | exact labelled trust quotient; tensor family has `p=(log_2 n)/2+1`, `sqrt(n)` bins and `O(sqrt(n)log n)` bits at bounded mass, with `O(n)` completion loss | strongest new near-original pass: a growing dense exact-sign interface has a strict state; arbitrary dense bridges need not satisfy closure |
| Rank-one common-factor one-port lift | fibre magnetization grid | rank-one blow-up / randomized cube rounding | derived and exactly audited | exact contraction identity; equal singleton `(G,R)` states retain `(3/32)rn` gap; a strict Hadamard factor hides `rn/2` in an orthogonal mode | useful boundary: rank-one fibres close, strict Hadamard fibres do not; the scalable family has `r=Theta(n)`, not the near-original `sqrt(n)` scale |
| Robust product-synchronized bridges | projective histogram plus a composable selector-error budget; relative pole defect for Cartesian growth | approximate common active face / tensor perturbation | derived and independently audited | fixed-arity `Delta_tau` is subadditive; Cartesian `D<=delta G` evolves as `1-prod(1-delta_i)`; coreset error is `rn(Delta/2+c eta)` | strong dynamic pass: joint cancellation survives perturbation; endpoint defect tables alone fail exact congruence by `48/64` versus `47/64` |
| Factorized pole phase | signed first moments of independent generator blocks plus relative defect | law of large numbers / tensor response state | derived and independently audited | max support error `sqrt(sum q_t^2)/p`; PC.3 completion tends to `3/4`; zero-defect alternating factors have cap subsequences `1` and `1/2` | strong near-original pass and sharp revision: within-scale synchronization and across-scale phase are independent obligations |
| Exact-sign five-port coherence | all odd products plus their joint majority Fourier combination | same-switch selector / regular-Hadamard tensor seed | derived and exactly verified | at the diagonal-completed roof every marginal deficit is `3/16` and majority defect is `1/2`; hollow exact-sign energies are unchanged and the valid-roof excess tends to `5/16`; no full-trust separation | scalable exact-sign energy-certificate falsifier; isolates growing arity plus vanishing marginal error as the live boundary |
| Switching-visible pole systems | projective orbit--character incidence plus signed edge-orbit defect | abelian switching twirl / multiplicity-free representation | derived and independently audited | `D<=(d/nu+sum_i sqrt(2k_i)/r)G`; hollow PC.3 is sharp at `2/5`; a non-Hadamard Cayley signing gives exact `nu=1` closure; PC.3 tensor visibility is `N^(-1/4)` | positive sign-specific rigidity theorem; visibility is checkable and smaller than full optimization, but can decay under growth and is not known for near-minimizers |
| PC.3 diagonal-switching tower | gap of the average pole-conjugated child plus selector-span closure | positive twirl / common top kernel | derived independently and exactly tested | `K_0^3=K_0`, so the tensor gap stays one and every selector defect is at most twice the mean product deficit at all depths | scalable exact-sign no-go: diagonal switching cannot realize vanishing marginals with constant coherent loss; genuinely non-switching children remain |
| Five-port seed amplification | quotient by the positive-roof monomial subgroup; full-product parity in the common-factor subclass | majority Fourier algebra / tensor Rayleigh product | derived and independently audited | monomial direct mixtures reproducing the concrete selector have `max_B d_B>=3/32`; the common-factor subclass has `d_full>=(3/8)d_sel` with stable error `11eta/8`; recursive products drive one marginal to one | scalable architecture no-go: the fixed exact-sign seed cannot be diluted into the desired asymptotic falsifier by these natural operations |
| Sparse-flip PC.3 coherence | diffuse selector plus a balanced target-field compiler | targeted edge flips / spherical trust bound / rowwise microcanonical rounding | derived and independently audited | active poles have `o(1)` loss but labelled response gap `>0.0146`; a uniform affine endpoint law upgrades this to unconstrained exact-sign parents with fixed `N^(3/2)` cap separation | A-level near-original contextual collision; multi-selector packing is next, with no claim about minima |

## Current prediction

Across the independently frozen finite-port benchmarks, the framework
recovers the classical state by the same operation: take the realizable image
of the conditional response vector and quotient only directions invisible to
all declared futures.  Its small-error exponent is the dimension of that
realizable projective image, not raw separator width.  Reuse under arbitrary
depth additionally requires a congruence or invariant arithmetic carrier.

The bridge ladder introduces a different transition.  Low rank and common
optimizer sections produce strict quotients; sparsity without a small live
separator does not; a generic dense sign bridge exposes extensive information
at the same scale as the motivating problem. Complete sign quadratics still
carry an extensive response rate, although they have a strict polynomial-bit
coefficient representation. The full exact cap-`1/2` Walsh switching orbit
also carries a linear response rate.  A Rayleigh-separated linear-rate
subcode now has a bounded-cap exact-sign one-port compiler, proving that
extensive response information alone does not prohibit low-cap metric
realization.  What remains is reusability. Under repeated Walsh composition, the
smaller bias/pair-overlap state fails by an extensive commutation-holonomy
gap. The remaining question is whether a joint gauge-covariant state can
transport the structured truth table without reconstructing the Boolean
maximum, and whether any such structure is forced for near-minimizers.

The Boolean port suite now resolves one controlled version of that question.
The exact state is the full projective row histogram, its fixed-scale response
entropy is only linear in port count, and its dynamic reuse is governed by
independence or mixing rather than static dimension.  Odd product closure
then synchronizes the histogram with a dense quadratic child and gives a
growing `O(sqrt(n)log n)`-bit exact carrier.  The equal-`(G,R)` collision is
the mandatory boundary: second-order geometry cannot substitute for this
higher-product algebra.  No theorem currently forces the algebra for
near-minimizers or realizes it at arbitrary balanced orders, so this is a
near-original benchmark success rather than a recurrence for `M_n`.

The branching benchmark records a second kind of successful state. It is not
an exact finite contextual quotient: critical centering and a vanishing
martingale coordinate create a scalar limiting Cox intensity. This keeps the
rare-event branch mathematically distinct from finite-port response geometry.
